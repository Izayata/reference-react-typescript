# Codebase Audit 3 — reference-react-typescript

**Date:** 2026-07-28 (last re-verified 2026-07-28 — after the §1 fixes landed: `FoodDetails.tsx` switched to the shared `useModal()` context (closing the dead-end double-modal bug and a related stuck-loading edge case), `Menu.tsx`'s grid grouping corrected to match its CSS and its `Allergen.tsx` sibling, `Checkout.tsx`'s redirect moved into a `useEffect`, `UsernameInput`/`EmailInput`'s availability-check cleanup made real (plus a stale-response guard), and the remaining message-content/typo/dead-code items (§1.5-1.9) fixed — **all of §1, Correctness bugs, is now fixed**, aside from the informational `checkPasswordIsCommon` note, left open by design since it's dead code), and after the §2 fixes landed: `AddressInput.tsx`'s own nested `<form>` (plus two related handler-less/duplicate `<form>`s at other call sites) converted to plain `<div>`s, closing the Enter-key-discards-everything bug across all 5 flows; `ShoppingBagButton` made keyboard-operable to match its already-fixed `HamburgerMenuButton` sibling; `InputFieldCheckmark`/`InputFieldXmark` now take a required, context-specific `ariaLabel` prop instead of a hardcoded username-only default (fixing `PasswordInput`/`ConfirmPasswordInput`, and `EmailInput` too, once investigating turned up the same underlying bug there); `Modal` now restores focus to whatever triggered it on close; the mobile hamburger menu now has a real, cycling focus trap; and `LoadingOverlay` now announces itself to screen readers — **all of §2, Accessibility, is now fixed**), and after the §3 fixes landed: `PersistSelectedStates` now uses `{ replace: true }` instead of pushing a duplicate history entry on every page load, `fetchCsrfToken` now validates the response actually contains a `csrfToken` before returning it, and `MyUserModelBuilder`'s dead `password` field (plus its paired dead test fixtures, plus one more unrelated unused import found in the same file) are removed — **all of §3, Code quality & architecture, is now fixed**, aside from the informational `NavLinkPersist`/`NavigatePersist` note, left open by design since it's dead code), and after the §4 fixes landed: 18 new test files (96 new tests) added first-ever coverage for the entire converter and myDecorators layers plus 3 previously-untested builders and 4 previously-untested models, every one of the 18 target files now at 100% statement/branch/function/line coverage — **all of §4, Testing & coverage, is now fixed**.)
**Scope:** Full repository, re-assessed independently of `AUDIT.md` and `AUDIT-2.md`. Both prior passes are extensive and, per their own documentation, essentially fully resolved (`AUDIT.md`: all sections fixed except two intentionally-deferred breaking-change items and one still-open correctness bug; `AUDIT-2.md`: all 8 sections fixed except one still-open correctness bug, §1.7, and a couple of by-design informational items). Re-ran the full baseline (`npx tsc --noEmit`, `npm run lint`, `CI=true npm test -- --watchAll=false`, `npm run test:coverage`, `CI=true npm run build`, `npm audit`) at the start of this pass — all clean/unchanged from what `AUDIT-2.md` documents (see the Appendix). No regressions found in previously-fixed work.
**Methodology:** A third, genuinely independent pass — every finding below was found by reading current source directly (not by re-running either prior audit's checklist) and cross-referenced against both `AUDIT.md` and `AUDIT-2.md` before being reported, to guarantee nothing here duplicates an already-fixed or already-documented-open finding. Coverage: every page component and its route wrapper, the full domain model layer (`model/`, `builder/`, `converter/`, `myDecorators/`, `utils/`), all form inputs and functional/navigation/header/footer components, the `supports/` cross-cutting helpers, and a fresh security/secrets/CORS spot-check. Each candidate finding was manually verified by reading the actual file (and, where relevant, live-running the specific Jest test or checking the installed `react-router` source) before being included — nothing here is guessed or inferred from a pattern alone.

This audit found **17 new, verified issues** the first two passes' methodology didn't catch — most concentrated in two places: five call sites share a genuine, previously-undocumented **nested-`<form>`** bug that silently discards in-progress checkout/registration/profile-edit data on an Enter keypress, and a scattering of accessibility gaps that mirror fixes `AUDIT.md`/`AUDIT-2.md` already made *elsewhere* but missed on an identically-styled sibling component (the exact "one outlier among near-identical siblings" pattern that caught real bugs in both prior passes). A few findings are real bugs in code that isn't currently reachable by any live user flow (no admin UI exists yet to create food items, and one utility function is never called at all) — these are flagged as such rather than overstated.

---

## Executive summary

- ~~**A completely unrecoverable modal on `FoodDetails`.** If loading a food's details fails for any reason, the page renders two stacked `<Modal>`s in sequence; dismissing the first exposes the second, whose own dismiss button calls `setModalMessage(null)` on state that's *already* `null` — React bails out of the update, so the second modal can never be closed.~~ **✅ Fixed** — see §1.1. Now uses the shared `useModal()` context every sibling page uses, making the double-modal dead end structurally impossible.
- ~~**Nested `<form>` elements silently discard in-progress form data on Enter.** `AddressInput` renders its own `<form>`, and every one of its 5 callers (guest/authenticated checkout, registration, profile billing/shipping address) wraps it in *another* `<form>`. Per the HTML form-ownership spec, pressing Enter in a Zip/City/Street/House-number field submits the *inner*, handler-less form — triggering a full-page GET reload that discards everything the user typed.~~ **✅ Fixed** — see §2.1. `AddressInput`'s inner form (and two other handler-less/duplicate ones found along the way) are now plain `<div>`s; confirmed in a real browser that Enter no longer reloads the page or discards typed data.
- ~~**The shopping-bag/checkout dropdown toggle is entirely unreachable by keyboard** — the identical bug `AUDIT.md` §6.3 already found and fixed on the hamburger menu button next to it in the same nav bar, missed on this one.~~ **✅ Fixed** — see §2.2. Now keyboard-operable with the same attributes its sibling already has; confirmed reachable via Tab in a real browser.
- ~~**`UsernameInput`/`EmailInput`'s availability-check cleanup doesn't actually run.** A `return () => clearTimeout(...)` sits inside a `setTimeout` callback instead of the `useEffect` callback — `setTimeout` discards it, so a stale in-flight availability check for a previous value isn't cancelled and can overwrite the checkmark/xmark shown for whatever the user has since typed.~~ **✅ Fixed** — see §1.4. Cleanup is now real, plus a `cancelled` guard against an already-in-flight stale response; confirmed via a new regression test.
- ~~**A duplicate history entry is pushed on every single page load**, silently breaking the browser Back button app-wide (users have to press it twice) — `PersistSelectedStates` unconditionally calls `setSearchParams` on mount, which `react-router` treats as a `push`, not a `replace`.~~ **✅ Fixed** — see §3.1. Now passes `{ replace: true }`; confirmed in a real browser that a single Back press correctly leaves the page.
- ~~**`Modal` never restores focus** to whatever triggered it once closed — every one of the 30+ `<Modal>` call sites app-wide leaves keyboard/screen-reader users dropped on `<body>` after dismissing an error.~~ **✅ Fixed** — see §2.4. Now captures and restores the previously-focused element on close; confirmed via a new regression test.
- ~~Two food-name validation error messages describe the wrong rule (claiming digits are allowed when they aren't; calling a food name "the description") — real bugs, but currently unreachable, since no admin UI exists yet to create/edit food items (the same gap `AUDIT-2.md` §8.2 already documents as by-design/not-yet-built).~~ **✅ Fixed** — see §1.5. Both messages now describe the actual rule correctly, ready for whenever an admin UI is eventually built.

---

## Findings

### 1. Correctness bugs — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 1.1 | `src/main/components/page/FoodDetails/FoodDetails.tsx:50-56` | **High — ✅ Fixed** | Local `modalMessage` state (this is the *only* page component that manages its own `<Modal>` instead of the shared `useModal()` context every sibling page uses) creates a genuine dead end. On a failed `/v1/foods/{foodId}` fetch (missing `foodId`, 404, network error), the `catch` sets `modalMessage` to an error string, and the render returns `<Modal message={modalMessage} onClose={() => setModalMessage(null)}/>` (line 50-52). Clicking OK sets `modalMessage` back to `null` and re-renders — but `food` is still `null` (the fetch never succeeded), so the very next render hits `if (!food) return <Modal message={t('foodDetails.fetchFailed')} onClose={() => setModalMessage(null)}/>` (line 54-56) and shows a **second** modal. That modal's own OK button also calls `setModalMessage(null)` — but state is *already* `null`, so React's `useState` setter bails out (`Object.is` check) and triggers no re-render. The second modal is now permanently stuck on screen with no way to dismiss it from inside the component. Concrete repro: navigate to `/food/999999` (nonexistent id) or hit any transient network failure loading a food's details — the user is stranded on an undismissable modal, recoverable only via the nav bar (unrelated global chrome), not via anything the modal itself offers. **Fixed:** switched `FoodDetails.tsx` to the shared `useModal()` context — the same pattern every sibling page (`Menu`, `Allergen`, `Checkout`, etc.) already uses — instead of managing its own local `modalMessage` state and rendering its own `<Modal>`. Since `App.tsx` renders exactly one, app-wide `<Modal>` driven by that shared context, the double-modal dead end is structurally impossible now: there's only ever one `<Modal>` instance in the tree. As a natural consequence of the rewrite, also fixed a related bug in the same effect: the `!foodId` early-return branch called `setModalMessage(...)` but never `setLoading(false)`, so that path left the page stuck on `<LoadingOverlay/>` forever (the loading check runs *before* the modal-message check, so the modal never even got a chance to show) — it now calls `setLoading(false)` too. Live-verified in a real browser (dev server + Playwright): navigating to `/food/999999` shows exactly one modal (`role="dialog"` count is `1`), and clicking OK fully dismisses it (count goes to `0`) rather than exposing a second, stuck modal. |
| 1.2 | `src/main/components/page/Menu/Menu.tsx:74-80` (`sortIntoSectionOfFour`) vs. `src/main/components/page/Menu/css/menu-item-details-container.css:21-24` | **Medium — ✅ Fixed** | `sortIntoSectionOfFour` groups menu items into rows of **3** (`for (let i = 0; i < array.length; i += 3) { result.push(array.slice(i, i + 3)) }`), but the CSS sizing each item is `width: calc(25% - 2rem)` at the `≥1024px` breakpoint — authored for **4** items per row. The identically-named sibling function in `Allergen.tsx:64-70` correctly groups by **4** (`i += 4`), and its own CSS matches (`calc(95% / 4 - 2rem)`) — confirming the `Menu.tsx` version is the outlier, almost certainly a copy-paste-and-modify-without-renaming bug. `.menu-items-row` (the flex container) uses `flex-direction: row` with no `justify-content`/`flex-wrap` override at that breakpoint, so 3 items at 25% width sit left-aligned, leaving a permanent, reproducible ~25%-wide empty gap on the right of every row with ≥4 items — on the app's primary "browse the menu" page, at desktop width. **Fixed:** `sortIntoSectionOfFour` now groups by 4 (`i += 4`, `slice(i, i + 4)`), matching its own name, the already-correct `Allergen.tsx` sibling, and the CSS. Live-verified in a real browser (dev server + Playwright, viewport 1280×900, `/v1/foods/menu/RESTAURANT` mocked with 4 items via route interception): all 4 cards now sit on the same row, evenly spaced edge-to-edge (bounding boxes at x=32/340/648/956, each 292px wide) with no trailing gap — confirmed both via computed bounding boxes and a full-page screenshot. |
| 1.3 | `src/main/components/page/Checkout/Checkout.tsx:230-232` | **Medium — ✅ Fixed** | `if (foods.length === 0 && !loading) { navigate('/cart') }` ran directly in the render body, not inside a `useEffect` — the render-phase state-update anti-pattern the codebase's own `AccountRouteGuard` was specifically fixed to avoid (`AUDIT.md` §2.5). Live-verified (pre-fix): running the existing `Checkout.test.tsx` "redirects to /cart when the shopping cart is empty" test reproduced the exact React warning `Cannot update a component (\`MemoryRouter\`) while rendering a different component (\`Checkout\`)`. **Fixed:** the redirect now runs inside a `useEffect` keyed on `[foods, loading, navigate]`, matching the pattern the rest of the app uses for this exact "redirect based on invalid state" scenario. Live-verified: re-running the same test now produces zero occurrences of the "Cannot update a component" warning (grepped the full test output), and the test itself still passes. |
| 1.4 | `src/main/components/input/myUser/UsernameInput/UsernameInput.tsx:34-48`, `src/main/components/input/myUser/EmailInput/EmailInput.tsx:36-50` | **Medium — ✅ Fixed** | The availability-check `setTimeout`'s own cleanup was never actually registered. `return () => clearTimeout(availabilityTimeout)` (line 48) was the return value of the **outer `setTimeout`'s callback function**, not of the `useEffect` — `setTimeout` discards whatever its callback returns, so that line was dead code. Only the outer `validationTimeout` (line 63's `return () => clearTimeout(validationTimeout)`) was ever a real, registered effect cleanup. Concrete failure scenario: user types `"testuser"`, pauses >1s (the outer debounce fires, value passes model validation, and 150ms later an availability fetch for `"testuser"` starts with no `AbortController`). Before that fetch resolves, the user types more (e.g. `"testuser1"`) — the effect re-runs for the new value, but the stale in-flight check for `"testuser"` was never cancelled, and its eventual resolution could overwrite the checkmark/xmark shown for the *current* value. **Fixed:** lifted `availabilityTimeout` to the effect's outer scope so its cleanup is now real (registered as part of the effect's actual `return () => {...}` — both timers are cleared), and added a `cancelled` boolean flag (set `true` in that same cleanup, checked before either `setAvailable`/`setError` call inside the async availability check) — the standard React pattern for guarding against a stale async response that resolves *after* the effect has already been cleaned up, which covers the case where the check had already started (not just merely scheduled) before the value changed. Applied identically to both `UsernameInput.tsx` and `EmailInput.tsx`. Added a new regression test to `UsernameInput.test.tsx` that starts an availability check for one value, changes the input to a second value before the first check resolves, then resolves the first (stale) check as "taken" — confirmed to fail against the pre-fix code (the stale response clobbers the UI, no more "available" checkmark) before confirming it passes against the fix. |
| 1.5 | `src/main/utils/food/FoodNameUtils.ts:4,7` vs. `src/main/i18n/locales/hu/hu.json:39-40` | **Low, currently unreachable — ✅ Fixed** | Two message-content bugs on the same model, both apparent copy-paste-from-`DescriptionModel` mistakes never updated for the field they actually describe: (a) `ERR_MSG_FOOD_NAME_VALUE_INVALID_CHARACTERS` read *"...csak betűket, **számokat** és szóközt..."* ("only letters, **numbers**, and spaces") but `FOOD_NAME_VALUE_ALLOWED_CHARACTERS` (`^[a-zA-ZáéíóöőúüűÁÉÍÓÖŐÚÜŰ]+...$`) has no `0-9` in its character class at all — a name like `"Pizza 4 sajtos"` was rejected with a message that says digits are fine. (b) `ERR_MSG_FOOD_NAME_VALUE_LENGTH` read *"**A leírás** legalább..."* ("**The description** must be at least...") on a food-**name** field — word-for-word identical to `ERR_MSG_DESCRIPTION_VALUE_LENGTH` except for the interpolated variable names, confirming the copy-paste origin. Currently unreachable (`FoodNameModel` is never actually constructed anywhere in production code — no admin UI exists yet to create/edit food items, per `AUDIT-2.md` §8.2), but real, tested-around code fixed now while the blast radius is zero. **Fixed:** `ERR_MSG_FOOD_NAME_VALUE_INVALID_CHARACTERS` no longer mentions "számokat" (now correctly describes only letters and spaces as allowed); `ERR_MSG_FOOD_NAME_VALUE_LENGTH` now says "Az ételnév" (the food name) instead of "A leírás" (the description). `FoodNameModel.test.tsx`'s assertions compare against the same imported `ERR_MSG_*` constants (not hardcoded literal text), so no test changes were needed — they automatically pick up the corrected message content. |
| 1.6 | `src/main/utils/customer/address/CityUtils.ts:6,10` vs. `src/main/utils/customer/address/StreetUtils.ts:6,9` | **Low — ✅ Fixed** | `CITY_VALUE_ALLOWED_REGEX` and `STREET_VALUE_ALLOWED_REGEX` are byte-for-byte identical patterns (both include `0-9`), both live and reachable via the real address-entry forms (`CityInput`/`StreetInput`, used in Register, Checkout, and `UserProfile`'s Billing/Shipping address editors) — yet `ERR_MSG_STREET_VALUE_FORMAT` mentioned "számokat" (numbers) and `ERR_MSG_CITY_VALUE_FORMAT` didn't. **Fixed:** `ERR_MSG_CITY_VALUE_FORMAT` now mentions "számokat" too, matching `ERR_MSG_STREET_VALUE_FORMAT`'s phrasing for the identical underlying rule. |
| 1.7 | `src/main/i18n/locales/hu/hu.json:94` (`ERR_MSG_STREET_NUMBER_VALUE_FORMAT`) | **Low — ✅ Fixed** | Hungarian typo: *"A házszám nem **tertalmazhat** szóközt..."* — should be "tartalmazhat" (contain). **Fixed:** corrected the spelling. |
| 1.8 | `src/main/components/page/ForgottenPassword/ForgottenPassword.tsx:135` | **Low — ✅ Fixed** | `resetPassword`'s `finally` block called `sleep(500)` without `await`ing it — a fire-and-forget call that accomplished nothing, since `setLoading(false)` on the next line ran immediately rather than after the intended 500ms. **Fixed:** added the missing `await` (the enclosing function is already `async`, confirmed by its own `await fetch(...)` a few lines above). |
| 1.9 | `src/main/components/page/Register/Register.tsx:149` | **Low — ✅ Fixed** | `<div className="form-container" onSubmit={handleSetAddressesStep}>` — `<div>` elements don't emit `submit` events, so this handler could never fire through this element; it was dead code. The real submission is correctly wired via `RegisterFormAddresses.tsx:24`'s own `<form onSubmit={handleSetAddressesStep}>`. **Fixed:** removed the dead `onSubmit` prop from the `<div>`; `handleSetAddressesStep` remains in use, passed down to `RegisterFormAddresses` as before. |
| — | `src/main/utils/myUser/PasswordUtils.ts:16-27` (`checkPasswordIsCommon`) | **Informational, unchanged** | `if (!res.ok) return false` on the common-password check — the identical fail-open bug class `AUDIT-2.md` §1.3 already fixed for `checkUsernameExists`/`checkEmailExists` (a failed check is silently reported as "safe" rather than "check failed"). Not fixed here: `checkPasswordIsCommon` is still never called anywhere in `src/main` or `src/test` (re-confirmed, zero call sites) — `PasswordInput.tsx` has no common-password check wired in at all. Left as-is, consistent with its original framing: apply the same fix pattern §1.3 already established *when* this is eventually wired up, rather than fixing dead code no path can currently reach. Cross-listed under §5.1 (Backend API contract). |

**Fixed:** all 9 numbered findings in this section addressed; the informational `checkPasswordIsCommon` note remains open by design (dead code, nothing to fix yet). Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 50 suites (490 tests, up from 489 — the new §1.4 regression test); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `npm run test:coverage` at `61/45.87/56.96/61.85`, still passing the `59/43/54/59` threshold; `CI=true npm run build` exits `0`. §1.1's and §1.2's fixes were both manually verified in a real browser (dev server + Playwright) — §1.1 by confirming a single dismissable modal instead of a stuck second one; §1.2 by mocking the menu API response with 4 items and confirming they now lay out edge-to-edge in one row with no gap, both by computed bounding boxes and a screenshot. §1.3's fix was verified by re-running the specific test that reproduced the original React warning and confirming the warning no longer appears. §1.4's fix has a dedicated new regression test, confirmed to fail against the pre-fix code before confirming it passes against the fix.

### 2. Accessibility — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 2.1 | `src/main/components/input/customer/address/AddressInput.tsx:23` (own `<form>`, no `onSubmit`/`method`/`action`), nested inside an outer `<form>` at 5 call sites: `src/main/components/page/UserProfile/BillingAddress/BillingAddress.tsx:153`, `ShippingAddress/ShippingAddress.tsx:153` (same pattern), `src/main/components/page/Register/form/RegisterFormAddresses.tsx:24` + `RegisterFormShippingAddress.tsx:35` (a *second*, empty `<form>` wrapping `AddressInput` — 3 levels deep for registration's shipping-address step), `src/main/components/page/Checkout/CheckoutCustomerDetailsSection.tsx:132,141` (guest and authenticated checkout, wraps `AddressInput` for both billing and shipping) | **High — ✅ Fixed** | `AddressInput` renders its own `<form>` around its five field inputs, and every single caller *also* wraps `<AddressInput>` in its own outer `<form>`. React builds these as real, live-nested DOM nodes (unlike HTML parsed from a string, where a parser would silently drop the inner `<form>` start tag). Per the HTML living standard, a text input's "form owner" for implicit Enter-key submission is its **nearest ancestor `<form>`**, not the outermost one — so pressing Enter in any Zip/City/Street/House-number/Floor-door field submitted `AddressInput`'s own inner, handler-less form (no `onSubmit`, no `method`, no `action`) instead of the outer form holding the real save/submit handler. A form with no `method`/`action` defaults to a **GET navigation to the current URL** with its fields serialized as query params — a full page reload that silently discarded all in-progress form state. Concrete repro: fill in the guest checkout form (`CheckoutCustomerDetailsSection.tsx`'s `!isAuthenticated` branch — note there's no submit button inside this `<form>` at all; the real "Megrendelés" button lives in a sibling component, `CheckoutOrderSummarySection.tsx`, outside this DOM subtree) and press Enter after typing a zip code, out of ordinary habit — the browser reloaded the page and silently discarded the entire checkout form. The same happened on Register's address step (3 levels deep for shipping) and on `UserProfile`'s Billing/Shipping address edit forms. `AUDIT.md` §3.8's closing note only mentions a nested-`<form>` console warning was observed as pre-existing during an unrelated check — it was never traced to a file/line or analyzed for functional impact; this finding filled that gap with the actual mechanism and its real, five-flow blast radius. **Fixed, three parts:** (1) `AddressInput.tsx`'s own `<form>` (which only ever used its `className` for styling, no `onSubmit`/`method`/`action` of its own) is now a plain `<div>` — this alone removes the nesting at all 5 originally-cited call sites, since each of those already has, or now has, exactly one real ancestor `<form>` with a working `onSubmit`. (2) `RegisterFormShippingAddress.tsx`'s own extra, empty `<form>` (no attributes at all, wrapping `AddressInput` a second time, the cause of the "3 levels deep" case) is now also a `<div>` — its sibling `RegisterFormBillingAddress.tsx` never had this extra wrapper to begin with. (3) The guest-checkout `<form>` in `CheckoutCustomerDetailsSection.tsx:141` had no `onSubmit` of its own either (the real submit button lives outside it, in a sibling component) — even after fixes (1)/(2) removed the *nested* form, this outer form would still have caught an Enter keypress and triggered the same GET-reload on its own, so it's now a `<div>` too. None of the three swapped elements had any tag-specific (`form.classname`) CSS selectors targeting them (confirmed via a full grep of `src/main/css/**` and their own local `css/` files) — only class selectors, which still apply identically to a `<div>`. Live-verified in a real browser (dev server + Playwright): filled the guest checkout's zip field and pressed Enter — URL stays on `/checkout` (no reload, no query-string GET params appended) and the typed value survives, versus the pre-fix reload-and-discard behavior. |
| 2.2 | `src/main/components/navigation-bar/components/shopping-bag/button/ShoppingBagButton.tsx:32-36` | **High — ✅ Fixed** | `<FontAwesomeIcon icon={faBagShopping} className='button-scale' onClick={...}/>` — no `role`, no `tabIndex`, no `onKeyDown`, no `aria-label`, no `aria-expanded`. This is the identical anti-pattern `AUDIT.md` §6.3 already found and fixed on the sibling `HamburgerMenuButton` (which correctly has `role="button"`, `tabIndex={0}`, a matching `onKeyDown`, `aria-expanded`, and `aria-label`), in the same nav bar, missed on this one. A keyboard-only user Tabbing through the nav bar skipped the shopping-bag icon entirely, so there was **no way to open the cart/checkout dropdown from the keyboard**. **Fixed:** added the identical set of attributes `HamburgerMenuButton` already has — `role="button"`, `tabIndex={0}`, an `onKeyDown` handler firing on Enter/Space, `aria-expanded={shoppingBagOpen}` (reflecting the real toggle state, since unlike the hamburger button this one *does* toggle open/closed rather than only opening), a new translated `aria-label` (`nav.openCartAriaLabel`, "Kosár megnyitása"), and `aria-controls="shopping-bag-dropdown"` (a new `id` added to `ShoppingBagDropdown.tsx`'s root `<div>` so the reference resolves to something real). Live-verified in a real browser (dev server + Playwright): Tabbed through the nav bar from a blank focus state, confirmed the shopping-bag button (identified by its new `aria-label`) receives focus via Tab, and pressing Enter on it opens the dropdown (`#shopping-bag-dropdown` becomes visible). |
| 2.3 | `src/main/components/input/myUser/InputFieldCheckmark/InputFieldCheckmark.tsx:7`, `InputFieldXmark/InputFieldXmark.tsx:7` — both take no props, hardcode `aria-label={t('inputs.usernameAvailableAriaLabel')}`/`usernameUnavailableAriaLabel` — reused by `src/main/components/input/myUser/PasswordInput/PasswordInput.tsx:93,96` and `ConfirmPasswordInput/ConfirmPasswordInput.tsx:78,81` | **Medium — ✅ Fixed** | These two generic indicator components are reused to show password validity/match state, but their `aria-label` was hardcoded to a username-specific Hungarian string ("Elérhető felhasználónév" / "Nem elérhető felhasználónév" — "Available/unavailable username") regardless of context. A screen-reader user typing a valid password on Register, `LoginData`'s change-password form, or `ForgottenPassword`'s reset form heard **"Available username"** once they paused — confusing and flatly wrong in a password-only context with no username field on screen. Investigating further while fixing this surfaced the same underlying issue also applied to `EmailInput.tsx`, not just the two password inputs the finding named — it reused the identical username-specific label too (less jarring than the password case, but still wrong), so it's fixed as part of the same change. **Fixed:** both `InputFieldCheckmark`/`InputFieldXmark` now take a required `ariaLabel: string` prop instead of hardcoding one internally — every caller must now be explicit, so there's no silent default to forget. Added four new context-specific translated label pairs to `hu.json` (`emailAvailableAriaLabel`/`emailUnavailableAriaLabel`, `passwordValidAriaLabel`/`passwordInvalidAriaLabel`, `passwordsMatchAriaLabel`/`passwordsMismatchAriaLabel` — the last pair specifically for `ConfirmPasswordInput`, which semantically checks "do the two passwords match," not "is this password format valid," so it gets its own distinct wording) alongside the existing username pair, and updated `UsernameInput`, `EmailInput`, `PasswordInput`, and `ConfirmPasswordInput` to each pass their own correct label. `EmailInput.test.tsx`'s two assertions that checked for the (wrong, borrowed) username label updated to the new email-specific text. |
| 2.4 | `src/main/components/functional/Modal/Modal.tsx` (whole component) | **Medium — ✅ Fixed** | `AUDIT.md` §6.1 added auto-focus onto the OK button and a focus trap while open — a real, verified fix — but nothing captured `document.activeElement` before the modal mounted, and nothing restored it on `onClose`. When the OK button (the modal's only focusable element) unmounted, the browser reset focus to `<body>` by default, forcing a keyboard/screen-reader user to re-navigate from the top of the page after dismissing any of the 30+ `<Modal>` call sites app-wide. **Fixed:** the same `useEffect` that auto-focuses the OK button now also captures `document.activeElement` (the element focused right before the modal mounted) into a local variable, and its cleanup function (which runs on unmount, i.e. exactly when the modal closes) calls `.focus()` on it. Added a new regression test to `Modal.test.tsx` (`'restores focus to the element that triggered it once closed'`) using a small trigger-button-plus-conditionally-rendered-modal test harness — confirmed to fail against the pre-fix code (focus stayed on `<body>`/nowhere) before confirming it passes against the fix. |
| 2.5 | `src/main/components/navigation-bar/navigation-menu/NavigationMenu.tsx:34-40` | **Medium — ✅ Fixed** | The open mobile hamburger menu is a full-viewport overlay (`position: fixed; width:100%; height:100vh` per `menu-container.css`, confirmed dismissible via Escape per `AUDIT.md` §6.4), but unlike `Modal.tsx` it had no focus trap, and nothing marked the rest of the page `aria-hidden`/`inert` while it was open. On a narrow viewport, a keyboard user could Tab past the last of the menu's four links into whatever's next in DOM order outside the menu (e.g. `Footer`'s links) — content visually hidden behind the opaque backdrop but still fully focusable. **Fixed:** added a real, cycling focus trap (unlike `Modal.tsx`'s single-element "always refocus the one button" trap, this menu has multiple real links a user needs to Tab between) — a `containerRef` on the menu's root `<div>`, and a `Tab`/`Shift+Tab` handler (merged into the same `keydown` listener that already handles `Escape`) that queries all currently-focusable elements inside the container and wraps focus from the last back to the first (or first back to last on `Shift+Tab`) rather than letting it escape into the page behind the backdrop. Added a new test file, `NavigationMenu.test.tsx` (3 tests: Escape closes the menu; Tab on the last link wraps to the first; Shift+Tab on the first link wraps to the last) — this component had zero test coverage before. The two focus-trap tests were confirmed to fail against the pre-fix code before confirming they pass against the fix. |
| 2.6 | `src/main/components/functional/LoadingOverlay/LoadingOverlay.tsx` | **Low — ✅ Fixed** | No `role="status"`, `aria-live`, `aria-busy`, or visually-hidden "Loading…" text anywhere — just two `<div>`s and an SVG spinner. This component gates nearly every page in the app. A screen-reader user got no announcement that anything was loading — the page just appeared to have no content for that duration. **Fixed:** added `role="status"`, `aria-live="polite"`, and `aria-busy="true"` to the overlay's icon-wrapping `<div>`, plus a visually-hidden (clipped to a 1px box, standard `sr-only`-style inline technique — no such utility class existed in this codebase to reuse) `<span>` with a new translated `common.loading` string ("Betöltés…"), so a screen reader announces that content is loading even though nothing changes visually. |

**Fixed:** all 6 findings in this section addressed. Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 51 suites (494 tests, up from 490 — 4 new tests: 1 in `Modal.test.tsx` for §2.4, 3 in a new `NavigationMenu.test.tsx` for §2.5); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `npm run test:coverage` at `61.55/46.52/57.26/62.42`, still passing the `59/43/54/59` threshold; `CI=true npm run build` exits `0`. §2.1 and §2.2 were additionally verified in a real browser (dev server + Playwright): §2.1 by filling the guest checkout's zip field and confirming Enter no longer reloads the page or discards the typed value; §2.2 by Tabbing through the nav bar and confirming the shopping-bag button now receives focus and opens its dropdown on Enter. §2.4 and §2.5 each have a dedicated regression test, both confirmed to fail against the pre-fix code before confirming they pass against the fix.

### 3. Code quality & architecture — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 3.1 | `src/main/supports/Persistence.tsx:8-20` (`PersistSelectedStates`) | **Medium — ✅ Fixed** | `useEffect(() => { setSearchParams(searchParams) }, [])` ran unconditionally on mount, re-setting the URL's query params to what they already are. Verified against the installed `react-router` source (`node_modules/react-router/dist/development/chunk-KS7C4IRE.mjs`): `setSearchParams(nextInit, navigateOptions)` unconditionally calls `navigate("?" + newSearchParams, navigateOptions)`, and with `navigateOptions` `undefined` here, `navigate`'s default is a **push**, not a `replace` — regardless of whether the new params are identical in content to the current ones. `PersistSelectedStates` wraps the entire app (`src/index.tsx:23`), so every full page load/app mount pushed a spurious, duplicate history entry pointing at the exact same URL. Concrete effect: any user who loaded (or reloaded) a page and then clicked the browser's Back button once landed right back on the same page — they had to click Back twice to actually leave. **Fixed:** `setSearchParams(searchParams)` now passes `{ replace: true }` as its second argument, so this effect updates the current history entry in place instead of pushing a new one — a `replace` is also the semantically correct choice here regardless of the history-pollution bug, since no real navigation is happening (the URL doesn't change). Live-verified in a real browser (dev server + Playwright): navigated `/allergens` → `/gallery`, pressed Back once, and landed on `/allergens` as expected (pre-fix, the same sequence would have required two Back presses, since the second one would still be needed to get past `/gallery`'s own spurious duplicate entry). |
| 3.2 | `src/main/supports/fetch-utilities/fetchCsrfToken.ts:18-21` | **Low-Medium — ✅ Fixed** | Only `!csrfResponse.ok` was checked before reading `csrfData.csrfToken` — if the backend/proxy ever returned `200 OK` with a body that doesn't contain `csrfToken` (an empty `{}`, a differently-shaped body that still comes back `200`, a misconfigured proxy fallback page), this function returned `undefined` typed as `string`, with no error. That `undefined` then flowed into every caller's headers object (e.g. `LogoutButton.tsx:26`); `Headers`/fetch-init coerce an `undefined` header value to the literal string `"undefined"`, so the request was sent with `X-CSRF-TOKEN: undefined`, the backend's CSRF filter rejects it, and the user saw a generic failure with no indication the real cause was an upstream response missing the expected field. **Fixed:** added a check that `csrfToken` is actually a non-empty string after parsing the response body; if not, throws the same `ERR_MSG_CSRF_TOKEN_FETCH_FAILED` error the `!ok` branch already uses, so a malformed-but-`200` response now fails the same clear way a non-`200` response does instead of silently propagating `undefined`. Added a new regression test to `fetchCsrfToken.test.ts` (a `200` response with an empty body `{}`) — confirmed to fail against the pre-fix code (the promise resolved instead of rejecting) before confirming it passes against the fix. |
| 3.3 | `src/main/builder/MyUserModelBuilder.tsx:9` | **Low — ✅ Fixed** | `private password?: PasswordModel` was declared but there was no `setPassword()` method on the class anywhere, and `build()` never referenced it — dead, unsettable, unused. `MyUserModel`'s constructor only takes `email`/`myUsername`/`customer`; this field was inert leftover, likely from before password handling moved to `MyUserRegistrationModel`/`NewPasswordDetailsModel`. Its sibling test file, `src/test/model/MyUserModel.test.tsx:68-78`, compounded this: `ERR_MYUSER_PASSWORD_UNDEFINED`/`ERR_MYUSER_PASSWORD_NULL` builder-chain fixtures were defined but never referenced by any `it()` block — dead test fixtures paired with the dead builder field, which could mislead a future reader into assuming password-required validation is tested here when it demonstrably isn't. **Fixed:** removed the dead `password` field and its now-unused `PasswordModel` import from `MyUserModelBuilder.tsx`; removed the two dead test fixtures and their now-unused `PasswordModel`/`ERR_MSG_PASSWORD_REQUIRED` imports from `MyUserModel.test.tsx`. While cleaning up that same test file, also removed a separate, pre-existing unused import (`MyUserModel` itself — the class was imported but never actually referenced anywhere in the file, only ever appearing inside comments/string literals) found via the lint warnings this cleanup surfaced; same category of dead code as the rest of this finding, in the exact file it's already about. `npm run lint`'s warning count dropped from 182 to 177 as a direct result (3 `no-unused-vars` warnings for the dead builder-field-related imports, 1 `no-unused-vars` for the dead fixture itself, 1 more for the separately-found `MyUserModel` import). |
| — | `src/main/supports/Persistence.tsx:22-36,66-74` (`NavLinkPersist`/`NavigatePersist`/`mergeLink`) | **Informational, unchanged** | Neither `NavLinkPersist` nor `NavigatePersist` is imported/used anywhere else in the app (re-confirmed, grepped — only `PersistSelectedStates` is actually used, in `src/index.tsx`). Since they exist and are exported, worth noting they carry real latent bugs: `NavLinkPersist`/`NavigatePersist` do `to: \`${props.to}${search}\`` assuming `props.to` is always a plain string, but their own prop types (`NavLinkProps`/`NavigateProps`) allow an object `to` — which would stringify to `"[object Object]"+search`, a broken URL. Even for a string `to` that already contains its own `?query`, naive concatenation with `search` (which starts with `?`) produces a malformed double-`?` URL — unlike `useNavigatePersist`'s own `mergeLink` helper, which correctly merges via `URLSearchParams` (though `mergeLink` itself silently drops all but the last value for any repeated/multi-valued query key, via `Object.fromEntries`). Not fixed here, consistent with its original framing: nothing in the current app actually calls either function, so there's no live bug to fix — the same out-of-scope-since-dead-code precedent applied to §1's `checkPasswordIsCommon` note. |

**Fixed:** all 3 numbered findings addressed; the informational `NavLinkPersist`/`NavigatePersist` note remains open by design (unused, dead code — nothing to fix until it's actually wired up). Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 51 suites (495 tests, up from 494 — the new §3.2 regression test); `npm run lint` exits `0` with 177 problems (0 errors, down from 182 — see §3.3); `npm run test:coverage` at `61.6/46.73/57.26/62.46`, still passing the `59/43/54/59` threshold; `CI=true npm run build` exits `0`. §3.1 was additionally verified in a real browser (dev server + Playwright): navigating between two pages and pressing Back once now correctly returns to the previous page, instead of requiring two presses. §3.2 has a dedicated regression test, confirmed to fail against the pre-fix code before confirming it passes against the fix.

### 4. Testing & coverage — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 4.1 | `src/main/converter/**` (6 files: `AddressModelConverter.ts`, `MyUserRegistrationModelConverter.ts`, `NewPasswordDetailsModel.ts`, `PersonalDetailsConverter.ts`, `RegistrationModelConverter.ts`, `formDataConverter.ts`) and `src/main/myDecorators/**` (5 files: `NotBlank`, `NotNull`, `NotUndefined`, `ValidPhoneNumber`, `NoZeroNorZeroSlash`) | **Medium — ✅ Fixed** | Zero dedicated unit test files existed for either directory — despite `CLAUDE.md`'s own documented architecture explicitly describing `converter/`+`myDecorators/` as core, first-class layers of the Model→Builder→Converter→Utils pattern, on par with `model/`/`builder/`, which *do* have exhaustive per-field test coverage. These 11 files got only indirect, incidental coverage through component-level tests that happened to route data through them. **Fixed:** added 6 new test files under `src/test/converter/**` (one per converter file, mirroring the source path — `NewPasswordDetailsModel.test.ts` tests the *converter* function of the same name, not to be confused with the existing model-level `src/test/model/NewPasswordDetailsModel.test.tsx`) asserting on correct field mapping between raw form-data shapes and the resulting model instances, plus validation-error propagation from the underlying models — 21 tests total. Added 5 new test files under `src/test/myDecorators/**` (one per decorator), each using a small standalone test class annotated with just that one decorator and `class-validator`'s `validateSync` directly, isolating each decorator's own `validate()` logic from any specific model that happens to use it — 25 tests total, covering the documented behavior of each decorator including its actual quirks as coded (e.g. `NoZeroNorZeroSlash` only inspects string values and always passes non-strings; `NotNull`/`NotUndefined` are deliberately narrower than their names might suggest — each only rejects its own one specific case). Both directories are now at 100% statement/branch/function/line coverage. |
| 4.2 | `src/main/builder/PasswordChangeModelBuilder.tsx`, `MyUserModelBuilder.tsx`, `NewPasswordDetailsModelBuilder.tsx` | **Low-Medium — ✅ Fixed** | `PasswordChangeModelBuilder` had zero references anywhere under `src/test/**` — completely untested, directly or indirectly, unlike every other builder in the app. `MyUserModelBuilder` and `NewPasswordDetailsModelBuilder` each lacked their own dedicated `src/test/builder/**` file. **Fixed:** added `PasswordChangeModelBuilder.test.tsx` (4 tests — required-error cases for each field left unset, a valid build, and a check that its `setCurrentPassword(string)` setter correctly wraps the raw string into a real `PasswordModel`), `MyUserModelBuilder.test.tsx` (7 tests, following the same unset/null-per-field pattern already established for sibling builders like `PersonalDetailsModelBuilder.test.tsx`), and `NewPasswordDetailsModelBuilder.test.tsx` (5 tests, including the password-mismatch business-rule case). All three builders are now at 100% coverage. |
| 4.3 | `src/main/model/OrderModel.tsx`, `src/main/model/order/OrderItemModel.tsx`, `src/main/model/FoodDetailsModel.tsx`, `src/main/model/ResetPasswordRequestModel.tsx` | **Low — ✅ Fixed** | No `src/test/model/**` counterpart existed for any of these four. **Fixed:** added `OrderItemModel.test.tsx` (6 tests), `OrderModel.test.tsx` (8 tests — including a check that the model carries exactly `customer`/`orderItems`/`paymentType` and nothing else, directly guarding against `AUDIT-2.md` §8.1's undocumented-field bug ever silently reappearing), `FoodDetailsModel.test.tsx` (13 tests, following the same exhaustive per-field pattern as its sibling `MenuItemModel.test.tsx`), and `ResetPasswordRequestModel.test.tsx` (7 tests, mirroring its near-identical sibling `ForgottenPasswordRequestModel.test.tsx`, plus the blank/whitespace-only token cases `@NotBlank` requires that plain `@NotNull`/`@NotUndefined` siblings don't). All four models are now at 100% coverage. |

**Fixed:** all 3 findings in this section addressed — 18 new test files (96 new tests) across the converter, decorator, builder, and model layers, every one of the 18 target files now at 100% statement/branch/function/line coverage. Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 69 suites (591 tests, up from 51/495); `npm run lint` exits `0` with 210 problems (0 errors — the increase from 177 is entirely new, expected `no-explicit-any` warnings from `as any` casts in the new test files, the same established convention every other model/builder test in this codebase already uses); `npm run test:coverage` at `62.34/47.21/58.92/63.24`, comfortably above the `59/43/54/59` threshold (one run flaked on 4 unrelated, pre-existing timing-sensitive component tests — `Checkout`/`ShoppingCart`/`UsernameInput`/`EmailInput` — under the added CPU load of coverage instrumentation; a clean immediate re-run passed all 69 suites, confirming it was a one-off system-load flake, not a regression from this section's changes, none of which touched `src/main/**` behavior); `CI=true npm run build` exits `0`. No manual browser verification needed — every change in this section is a new `src/test/**` file with zero `src/main/**` behavior changes.

### 5. Backend API contract

| # | Location | Severity | Description |
|---|---|---|---|
| 5.1 | `POST /v1/registration/common-password` (`API_ENDPOINTS.md:67-78`), only ever called by `src/main/utils/myUser/PasswordUtils.ts:16-27`'s `checkPasswordIsCommon` | **Informational** | `AUDIT-2.md` §8.2 documents four backend endpoints as "documented but uncalled" (`POST /v1/foods`, `GET /v1/customers`, `GET /v1/orders/{id}`, `GET /v1/ingredients`). This is a fifth, narrower instance of the same category the prior pass's grep-for-`fetch()`-call-sites methodology didn't catch: `checkPasswordIsCommon` *does* exist and *does* call the documented endpoint correctly — but the function itself is never called from `PasswordInput.tsx` or anywhere else in the app (grepped, zero call sites). So while a `fetch()` call site referencing this URL technically exists in the codebase, it's dead code that never executes in the running app — the endpoint is, in effect, exactly as uncalled as the four `AUDIT-2.md` already lists. Cross-referenced from §1's `checkPasswordIsCommon` fail-open note above rather than duplicated. |

**Re-verified, no change:** every other `fetch()` call site's path/method still matches `API_ENDPOINTS.md` (re-confirmed no regressions since `AUDIT-2.md` §8's pass); request-body shapes for the endpoints `AUDIT-2.md` §8 already spot-checked remain correct.

### 6. Security & tooling — re-verified clean, no new findings

Re-ran the same clean-scan categories `AUDIT.md`/`AUDIT-2.md` §2 already covers: no `dangerouslySetInnerHTML`, `eval()`, or `new Function()` anywhere in `src/main`; `nginx.conf`'s CORS headers still use the `$cors_allow_origin` variable (no wildcard); no hardcoded secrets/API keys found via a fresh pattern scan. `npm audit` unchanged at 65 vulnerabilities (4 low, 1 moderate, 60 high), matching `AUDIT-2.md` §2.2's documented, already-investigated state — no new advisories since that pass. No new tooling/CI findings beyond `AUDIT-2.md` §5's already-fixed items.

---

## Summary table

| # | Finding | Severity |
|---|---|---|
| 1.1 | ~~`FoodDetails.tsx`'s own local-modal state creates a permanently-unclosable dead-end modal on any fetch failure~~ | ~~High~~ **✅ Fixed** — now uses the shared `useModal()` context, same as every sibling page |
| 2.1 | ~~Nested `<form>` elements across 5 address-entry flows submit the wrong (inner, handler-less) form on Enter, discarding in-progress data~~ | ~~High~~ **✅ Fixed** — nested/handler-less forms converted to `<div>`s, verified in a real browser |
| 2.2 | ~~`ShoppingBagButton` completely unreachable via keyboard — identical bug already fixed on the sibling hamburger button~~ | ~~High~~ **✅ Fixed** — now keyboard-operable, verified reachable via Tab in a real browser |
| 1.2 | ~~`Menu.tsx` groups items in rows of 3 while the CSS is sized for 4 — permanent visual gap on desktop~~ | ~~Medium~~ **✅ Fixed** — now groups by 4, verified in a real browser |
| 1.3 | ~~`Checkout.tsx` calls `navigate('/cart')` directly in the render body instead of a `useEffect`~~ | ~~Medium~~ **✅ Fixed** — moved into a `useEffect`, warning confirmed gone |
| 1.4 | ~~`UsernameInput`/`EmailInput`: availability-check cleanup never actually registered — stale checks can overwrite current field state~~ | ~~Medium~~ **✅ Fixed** — cleanup + stale-response guard added, regression test included |
| 2.3 | ~~`InputFieldCheckmark`/`InputFieldXmark` announce "username available/unavailable" when reused for password fields~~ | ~~Medium~~ **✅ Fixed** — now takes a required, context-specific `ariaLabel` prop (also fixed `EmailInput`) |
| 2.4 | ~~`Modal` never restores focus to the triggering element on close, app-wide~~ | ~~Medium~~ **✅ Fixed** — focus now restored, regression test included |
| 2.5 | ~~Mobile hamburger menu has no focus trap; background content stays focusable behind the backdrop~~ | ~~Medium~~ **✅ Fixed** — real cycling focus trap added, regression tests included |
| 3.1 | ~~`PersistSelectedStates` pushes a redundant duplicate history entry on every page load, breaking single-press Back~~ | ~~Medium~~ **✅ Fixed** — now uses `{ replace: true }`, verified in a real browser |
| 4.1 | ~~`converter/**` and `myDecorators/**` (11 files) have zero dedicated unit tests~~ | ~~Medium~~ **✅ Fixed** — 11 new test files, both directories now at 100% coverage |
| 3.2 | ~~`fetchCsrfToken` doesn't validate the response actually contains a `csrfToken` field~~ | ~~Low-Medium~~ **✅ Fixed** — now throws instead of returning `undefined`, regression test included |
| 4.2 | ~~`PasswordChangeModelBuilder` fully untested; `MyUserModelBuilder`/`NewPasswordDetailsModelBuilder` lack dedicated builder tests~~ | ~~Low-Medium~~ **✅ Fixed** — 3 new builder test files, all now at 100% coverage |
| 1.5 | ~~`FoodNameModel`: two validation-message content bugs (claims digits allowed when forbidden; says "description" not "food name")~~ | ~~Low, currently unreachable~~ **✅ Fixed** — both messages corrected |
| 1.6 | ~~`CityUtils`/`StreetUtils`: identical regex, but only one message mentions numbers are allowed~~ | ~~Low~~ **✅ Fixed** — `CityUtils`'s message now mentions numbers too |
| 1.7 | ~~Hungarian typo "tertalmazhat" (should be "tartalmazhat") in the house-number format error~~ | ~~Low~~ **✅ Fixed** — spelling corrected |
| 1.8 | ~~`ForgottenPassword.tsx`: unawaited `sleep(500)` in a `finally` block does nothing~~ | ~~Low~~ **✅ Fixed** — added the missing `await` |
| 1.9 | ~~`Register.tsx`: dead `onSubmit` prop on a `<div>`~~ | ~~Low~~ **✅ Fixed** — removed |
| 2.6 | ~~`LoadingOverlay` has no `aria-live`/`role="status"` — no non-visual loading announcement~~ | ~~Low~~ **✅ Fixed** — added `role="status"`, `aria-live`, `aria-busy`, and visually-hidden loading text |
| 3.3 | ~~`MyUserModelBuilder`'s dead `password` field, paired with dead, unreferenced test fixtures in `MyUserModel.test.tsx`~~ | ~~Low~~ **✅ Fixed** — dead field, fixtures, and imports all removed |
| 4.3 | ~~Missing model tests: `OrderModel`, `OrderItemModel`, `FoodDetailsModel`, `ResetPasswordRequestModel`~~ | ~~Low~~ **✅ Fixed** — 4 new model test files, all now at 100% coverage |
| — | `checkPasswordIsCommon` fail-open bug (same class as the already-fixed §1.3) — but dead code, never called | **Informational** |
| — | `NavLinkPersist`/`NavigatePersist` have real latent bugs but are unused anywhere in the app | **Informational** |
| 5.1 | `POST /v1/registration/common-password`: a 5th "documented but effectively uncalled" endpoint beyond `AUDIT-2.md` §8.2's four | **Informational** |

---

## Appendix: live tool output (this pass, 2026-07-28)

```
$ npx tsc --noEmit
(no output, exit code 0)

$ npm run lint
✖ 182 problems (0 errors, 182 warnings)   # unchanged from AUDIT-2.md's documented state;
                                            # all warnings are pre-existing src/test/** no-explicit-any
(process exit code: 0)

$ CI=true npm test -- --watchAll=false
Test Suites: 50 passed, 50 total
Tests:       489 passed, 489 total
(process exit code: 0 — unchanged from AUDIT-2.md's final documented state)

$ npm run test:coverage
All files | 61.1% Stmts | 45.55% Branch | 56.81% Funcs | 61.94% Lines
(process exit code: 0 — passes the 59/43/54/59 threshold; unchanged)

$ CI=true npm run build
Compiled successfully.
(process exit code: 0 — unchanged)

$ npm audit
65 vulnerabilities (4 low, 1 moderate, 60 high, 0 critical)
(unchanged from AUDIT-2.md §2.2's documented, already-investigated state)
```

No regressions found anywhere in this baseline — every number above matches what `AUDIT-2.md` already documents as the current, fixed state. All findings in this document are genuinely new, independently discovered by reading current source, and each was manually verified (by direct file inspection, and in three cases — §1.3's React warning, §3.1's `react-router` `navigate` behavior, and §2.2's side-by-side comparison with the already-fixed `HamburgerMenuButton` — by live-running the relevant test or reading the relevant installed package/sibling source) before being included here.

```
$ npx tsc --noEmit   # after the §1 fixes (1.1-1.9)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §1 fixes
Test Suites: 50 passed, 50 total
Tests:       490 passed, 490 total
(process exit code: 0; 489->490 is the new UsernameInput.test.tsx regression
 test added for §1.4, confirmed to fail against the pre-fix code first)

$ npm run lint   # after the §1 fixes
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ npm run test:coverage   # after the §1 fixes
All files | 61% Stmts | 45.87% Branch | 56.96% Funcs | 61.85% Lines
(process exit code: 0 — still passes the 59/43/54/59 threshold; the small
 shift from 61.1/45.55/56.81/61.94 is FoodDetails.tsx's refactor and the
 new regression test, not a regression)

$ CI=true npm run build   # after the §1 fixes
Compiled successfully.
(process exit code: 0)
```

§1.1 and §1.2 were additionally verified in a real browser (dev server + Playwright): §1.1 by confirming `/food/999999` now renders exactly one dismissable modal instead of a stuck second one; §1.2 by mocking `/v1/foods/menu/RESTAURANT` with 4 items and confirming they lay out edge-to-edge in one row (bounding boxes and a full-page screenshot both checked) instead of leaving a gap.

```
$ npx tsc --noEmit   # after the §2 fixes (2.1-2.6)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §2 fixes
Test Suites: 51 passed, 51 total
Tests:       494 passed, 494 total
(process exit code: 0; 490->494 is 1 new Modal.test.tsx regression test for
 §2.4 plus a new NavigationMenu.test.tsx (3 tests) for §2.5, both confirmed
 to fail against the pre-fix code before confirming they pass against the fix)

$ npm run lint   # after the §2 fixes
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ npm run test:coverage   # after the §2 fixes
All files | 61.55% Stmts | 46.52% Branch | 57.26% Funcs | 62.42% Lines
(process exit code: 0 — still passes the 59/43/54/59 threshold)

$ CI=true npm run build   # after the §2 fixes
Compiled successfully.
(process exit code: 0)
```

§2.1 and §2.2 were additionally verified in a real browser (dev server + Playwright): §2.1 by filling the guest checkout's zip field and confirming Enter no longer reloads the page or discards the typed value (URL and field value both checked before/after); §2.2 by Tabbing through the nav bar from a blank focus state and confirming the shopping-bag button (identified by its new `aria-label`) now receives focus and opens its dropdown on Enter.

```
$ npx tsc --noEmit   # after the §3 fixes (3.1-3.3)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §3 fixes
Test Suites: 51 passed, 51 total
Tests:       495 passed, 495 total
(process exit code: 0; 494->495 is a new fetchCsrfToken.test.ts regression
 test for §3.2, confirmed to fail against the pre-fix code before confirming
 it passes against the fix)

$ npm run lint   # after the §3 fixes
✖ 177 problems (0 errors, 177 warnings)   # down from 182 -- §3.3 removed 5
                                            # dead-code-related no-unused-vars
                                            # warnings from MyUserModel.test.tsx
(process exit code: 0)

$ npm run test:coverage   # after the §3 fixes
All files | 61.6% Stmts | 46.73% Branch | 57.26% Funcs | 62.46% Lines
(process exit code: 0 — still passes the 59/43/54/59 threshold)

$ CI=true npm run build   # after the §3 fixes
Compiled successfully.
(process exit code: 0)
```

§3.1 was additionally verified in a real browser (dev server + Playwright): navigated `/allergens` → `/gallery`, pressed Back once, and landed on `/allergens` as expected — pre-fix, the same sequence would have needed a second Back press to get past `/gallery`'s own spurious duplicate history entry.

```
$ npx tsc --noEmit   # after the §4 fixes (4.1-4.3)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §4 fixes
Test Suites: 69 passed, 69 total
Tests:       591 passed, 591 total
(process exit code: 0; 495->591 is 18 new test files: 6 in src/test/converter/**,
 5 in src/test/myDecorators/**, 3 new builder tests, 4 new model tests)

$ npm run lint   # after the §4 fixes
✖ 210 problems (0 errors, 210 warnings)   # up from 177 -- entirely new,
                                            # expected no-explicit-any warnings
                                            # from `as any` casts in the new
                                            # test files, the same established
                                            # convention every other model/
                                            # builder test already uses
(process exit code: 0)

$ npm run test:coverage   # after the §4 fixes
All files | 62.34% Stmts | 47.21% Branch | 58.92% Funcs | 63.24% Lines
(process exit code: 0 — comfortably above the 59/43/54/59 threshold; converter/**,
 myDecorators/**, and all 7 newly-tested builder/model files are now at 100%)
```

One `npm run test:coverage` run flaked on 4 unrelated, pre-existing timing-sensitive component tests (`Checkout`/`ShoppingCart`/`UsernameInput`/`EmailInput`, all exceeding their fixed 3000ms `findBy*` timeouts) under the added CPU load of coverage instrumentation (that run took 55s instead of the usual ~11s) — an immediate clean re-run passed all 69 suites, confirming a one-off system-load flake rather than a regression; none of this section's changes touched `src/main/**` behavior. No manual browser verification needed — every change in this section is a new `src/test/**` file only.
