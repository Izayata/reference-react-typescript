# Codebase Audit 2 — reference-react-typescript

**Date:** 2026-07-27 (last re-verified 2026-07-28 — after the §1.1 fix landed (`PersonalData.tsx`'s unreachable edit flow, plus its first-ever test coverage), after the remaining §1.2-1.6 fixes landed in the same pass: the dead "Kapcsolat" nav link now scrolls to real content and a real catch-all 404 page exists, the username/email availability-check fail-open/unhandled-rejection bugs are fixed, Checkout's shipping-address edit now actually persists to the backend, the `errorData.error`/`errorData.message` inconsistency is standardized on `message`, and `LogoutButton.tsx` now uses the same error-formatting utility as the rest of the app (**all originally-identified §1 findings are now fixed**), after the §2.1 fix landed: the guest-checkout CSRF over-send is gone, standardizing the app on the CSRF-exemption rules `API_ENDPOINTS.md` documents, and after §2.2/§2.3 — initially left deliberately open pending explicit authorization for their breaking-change risk — were revisited and fixed on explicit user request: `react-router-dom` upgraded to 7.18.1 (fixing the open-redirect CVE, zero application code changes, Node 18→20 bump required and verified via a real Docker build) and `npm audit`'s count reduced from 73 to 65 via targeted `overrides` on isolated leaf packages, with the remainder correctly left alone since their only "fix" would install a non-functional `react-scripts@0.0.0` — **all of §2, Security, is now fixed or partially fixed as far as safely possible**), after the §3 fixes landed: `fetchCsrfToken.tsx` renamed to `.ts` and its redundant `console.error` removed, the misleadingly-named `pages/Order/CustomerDetailsPage` moved to `pages/CheckoutPage` matching every sibling page's naming convention, and `Modal.tsx`'s dead `isSuccess` styling branch removed (which, as a bonus, also closed §7.1 — the exact same code was independently flagged there as a hidden locale-coupling landmine) — **all of §3, Code quality & architecture, is now fixed**), and after the §4 fixes landed: `UserProfile.tsx`, `BillingAddress.tsx`, `ShippingAddress.tsx`, and `LoginData.tsx` (the last four of §4.1's five 0%-covered account-page files) now have real test suites (74.28%/85%/85%/95% statement coverage respectively), `UsernameInput.tsx`/`EmailInput.tsx`'s previously-untested async availability-check block (including the exact code §1.3 fixed) is now covered (§4.2), `fetchCsrfToken.ts` went from 0% to 100% coverage including its error path (§4.3), and the `jest.coverageThreshold` floor was re-tightened from `45/30/43/46` to `59/43/54/59` to track the substantial coverage growth (live: 47.34/31.58/44.51/48.36 → 61.1/45.55/56.81/61.94) — **all of §4, Testing & coverage, is now fixed**. Writing `UserProfile.test.tsx` for §4.1 surfaced one new, still-open finding: §1.7, a fetch-error-handling bug in `UserProfile.tsx` (an `Error` object briefly lands in modal-message state instead of its `.message`, and a `setModalMessage` call happens during render) — deliberately not fixed as part of the testing pass that found it, per this document's established practice, and after the §5 fix landed: a new `.github/dependabot.yml` (npm/github-actions/docker, weekly) closes §5.1, the last open item in that section — **all of §5, Tooling & CI/CD, is now fixed**), and after the §6 fixes landed: `public/index.html`'s `<html lang="en">` corrected to `lang="hu"` (§6.1), and every `page-title`-classed heading that represents a page's own singular title (`NotFound`, `Allergen`, `UserProfile`, `FoodDetails`, plus the other halves of the `Checkout`/`ShoppingCart` state-toggling pairs) is now consistently `<h1>` (§6.2) — **all of §6, Accessibility, is now fixed**), and after the §7 fix landed: `fetchCsrfToken.ts`'s last hardcoded English string is now translated via a new `errors.ERR_MSG_CSRF_TOKEN_FETCH_FAILED` key, following the same free-function `i18n.t(...)` pattern already used elsewhere in the codebase — **all of §7, Internationalization, is now fixed or confirmed unchanged by design (7.3)**), and after the §8 fix landed: `OrderModel`'s undocumented `isAuthenticatedUser` field is removed entirely, so the order request body now matches `API_ENDPOINTS.md`'s documented `{ customer, orderItems, paymentType }` shape exactly, with a new regression test asserting the serialized body's exact key set — **all of §8, Backend API contract, is now fixed**.)
**Scope:** Full repository, re-assessed independently of `AUDIT.md`. `AUDIT.md` (the first audit pass) found a large set of issues across security, correctness, tooling, accessibility, and a full frontend/backend API cross-check — essentially all of it has since been fixed, verified by re-running `npx tsc --noEmit`, `npm run lint`, `CI=true npm test -- --watchAll=false`, `npm run test:coverage`, `CI=true npm run build`, and `npm audit` at the start of this pass (all clean/unchanged from what `AUDIT.md` documents — see the Appendix).
**Methodology:** This is a genuinely fresh pass, not a summary of `AUDIT.md`. Every finding below was independently re-derived by reading the current source and cross-referencing it against `API_ENDPOINTS.md`, the live test-coverage report, and the rendered app — not copied from the first audit's conclusions. Where a finding overlaps with something `AUDIT.md` already documents as fixed, it's called out explicitly as confirmed-still-fixed rather than restated as new.

This audit found a well-hardened codebase overall — the fixes from `AUDIT.md` held up, no regressions. But a fresh, independent read surfaced **9 real defects the first audit's methodology didn't catch** (plus a 10th, §1.7, surfaced later while fixing §4's test-coverage gaps), several of them concrete and user-facing (a completely unreachable profile-edit flow, a dead nav link, a wrong `<html lang>` on an otherwise fully-localized app), plus supporting evidence for *why* they weren't caught (the exact components involved have 0% test coverage).

---

## Executive summary

- ~~**`UserProfile`'s "Personal Data" edit flow is completely unreachable.** A single inverted condition means the only button that enters edit mode is itself hidden until edit mode is already on.~~ **✅ Fixed** — see §1.1. `PersonalData.tsx` had 0% test coverage (§4.1), which is almost certainly why this shipped unnoticed; a regression test (confirmed to fail against the bug before confirming it passes against the fix) now covers it. `UserProfile.tsx` and its other three subsections have since been covered too — see §4.1.
- **Writing that missing coverage for `UserProfile.tsx` (§4.1) surfaced a new bug, §1.7**: its `GET /v1/account/me` failure path passes a raw `Error` object to `setModalMessage` instead of its `.message`, and separately calls `setModalMessage` during render — two React anti-patterns that log console errors on every failed profile fetch, currently masked from users only because the second bug's render-loop side effect happens to overwrite the first bug's bad state before the final render commits. Not yet fixed — flagged here as a new, still-open finding.
- ~~**The "Kapcsolat" (Contact) nav link 404s to a blank page** — it points at `/contact`, but no such route (and no catch-all) exists in `App.tsx`.~~ **✅ Fixed** — see §1.2. The link now scrolls to the Footer's existing contact-info section instead, and a real catch-all 404 page now exists for any other unmatched route.
- ~~**The app's `<html>` tag says `lang="en"`, but 100% of the app's content is Hungarian** — every screen reader will attempt to pronounce Hungarian text with English phonetics.~~ **✅ Fixed** — see §6.1. Now `lang="hu"`; notable given the extensive `i18next` migration `AUDIT.md` §7 already did — this static-HTML piece was outside that migration's reach and had never been revisited until now.
- ~~**Two username/email-availability checks silently fail open.** `checkUsernameExists`/`checkEmailExists` treat *any* non-2xx response (including the documented 20/hr rate limit) as "not taken," and the async check itself sits outside the `try/catch` meant to catch it.~~ **✅ Fixed** — see §1.3. Failed checks now throw and are caught, distinguishing "check failed" from "confirmed available."
- ~~**Checkout's "edit shipping address" during checkout doesn't call the backend at all.** It mutates local state only, presented with the exact same edit/save UI the real (persisting) `UserProfile` version uses.~~ **✅ Fixed** — see §1.4. It now calls the same `PUT /v1/customer/shipping-address` endpoint the profile page uses.
- ~~**The frontend can't agree on the backend's error-response field name.** Two call sites read `errorData.message`, two read `errorData.error` — at least one pair is reading a field that isn't there and silently discarding the real backend error text.~~ **✅ Fixed** — see §1.5. Standardized on `errorData.message`, based on the field name used in `API_ENDPOINTS.md`'s own concrete response examples.
- ~~A guest checkout order still sends an `X-CSRF-TOKEN` to `POST /v1/orders/guest`, which `API_ENDPOINTS.md` documents as CSRF-exempt — the same category of bug `AUDIT.md` §8.3 already fixed elsewhere, just a fourth instance it didn't catch.~~ **✅ Fixed** — see §2.1. The token is now only attached for authenticated orders.
- ~~`react-router` is in a vulnerable open-redirect range, and `npm audit` reports 73 vulnerabilities with no non-breaking fix available.~~ **✅/Partially fixed** — see §2.2/§2.3, done with explicit authorization given the breaking-change risk. `react-router-dom` upgraded to 7.18.1 with zero application code changes and full browser verification (a companion Node 18→20 bump was required and verified via a real Docker build); `npm audit`'s count dropped from 73 to 65 via targeted `overrides` on three isolated leaf packages, with the rest correctly left alone since the only "fix" available (`npm audit fix --force`) would install a non-functional `react-scripts@0.0.0` and break the entire build toolchain.
- ~~`OrderModel` serializes an `isAuthenticatedUser` field into every order request body that isn't part of the documented request shape.~~ **✅ Fixed** — see §8.1. The field is removed entirely; the order body now matches `API_ENDPOINTS.md` exactly, backed by a new regression test.
- ~~One hardcoded, untranslated English string was genuinely reachable by real users: `fetchCsrfToken.ts`'s `throw new Error('Failed to fetch CSRF token')`, surfaced verbatim in an otherwise all-Hungarian modal on any CSRF-token network hiccup.~~ **✅ Fixed** — see §7.2. Now throws a translated `errors.ERR_MSG_CSRF_TOKEN_FETCH_FAILED` message via the same `i18n.t(...)`-outside-a-component pattern used elsewhere in the codebase.

---

## Findings

### 1. Correctness bugs — partially fixed (§1.7 newly discovered, not yet fixed)

| # | Location | Severity | Description |
|---|---|---|---|
| 1.1 | `src/main/components/page/UserProfile/PersonalData/PersonalData.tsx:120` (button) / `:129` (form) | **High — ✅ Fixed** | The view/edit toggle for personal data (firstname/lastname/phone number) is inverted. The data-display block is correctly gated on `{!editMode && (...)}` (line 104), but the "Edit" button that calls `handleEdit()` (which does `setEditMode(true)`) was gated on `{editMode && (...)}` (line 120) — and the edit `<form>` itself is *also* gated on `{editMode && (...)}` (line 129, correct as-is). Since `editMode` starts `false` and the only way to set it `true` was clicking a button that didn't render until it was already `true`, **there was no way to enter edit mode through the UI at all** — a logged-in user could never edit their name or phone number from the account page. Confirmed pre-existing since the repo's initial commit (`git log --follow` shows no revision ever had the correct condition). The three sibling components in the same directory (`BillingAddress.tsx:115`, `ShippingAddress.tsx:115`, `LoginData.tsx:117`) all correctly use `{!editMode && (<button onClick={handleEdit}>...` — this was the one outlier. **Fixed:** the button's condition at line 120 is now `{!editMode && (...)}`, matching the display block above it and all three sibling components. Added `src/test/components/page/UserProfile/PersonalData/PersonalData.test.tsx` (3 tests: view mode shows the current details and an Edit button but not the form, clicking Edit reveals the Save/Cancel form, clicking Cancel returns to view mode) — this is the first test coverage `PersonalData.tsx` has ever had (was 0%, per §4.1); the test was verified to fail against the pre-fix code (all 3 assertions fail when the bug is reintroduced) before confirming it passes against the fix, so it functions as a real regression test, not just a coverage-padding one. Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 43 suites (464 tests, up from 42/461); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `CI=true npm run build` exits `0`. Not manually re-verified in a real browser — `/account` requires real authentication against a live backend, which isn't available in this environment (same constraint as prior sessions' UI checks); the new automated test is the verification here, and it was specifically confirmed to catch the exact bug being fixed. |
| 1.2 | `src/main/components/navigation-bar/navigation-menu/navigation-link-menu/NavigationLinkMenu.tsx:63` (`to='/contact'`), `src/App.tsx:87-106` (`<Routes>`) | **High — ✅ Fixed** | The "Kapcsolat" (Contact) nav link — one of 4 primary nav items, present on every page — pointed at `/contact`. No `<Route path="/contact">` existed in `App.tsx`, and there was no catch-all (`path="*"`) route either. Clicking it rendered `Header`/`Nav`/`Footer` (all outside `<Routes>`) but a completely blank `<main>` — no content, no "not found" message. **Fixed, two parts:** (1) the "Kapcsolat" link now points at `#contact` (a plain anchor, not a route) and scrolls to the Footer's existing "Elérhetőségek" (contact info: email/phone/address) section — that content already existed and renders on every page (`Footer.tsx`, outside `<Routes>`), so this reuses real content instead of inventing a new page; added `id='contact'` to that Footer `<section>`. (2) Added a real catch-all route (`<Route path="*" element={<NotFoundPage />} />` in `App.tsx`) backed by a new `NotFound`/`NotFoundPage` component pair (following the existing thin-route-wrapper convention), so any *other* future dead link shows a proper "not found" page instead of a blank one. Live-verified in a real browser: clicking "Kapcsolat" scrolls to the visible, populated `#contact` section; navigating to an arbitrary unmatched path renders the new 404 page with its title, message, and a working "Vissza a főoldalra" (back to home) link. |
| 1.3 | `src/main/components/input/myUser/UsernameInput/UsernameInput.tsx:38-39`, `src/main/components/input/myUser/EmailInput/EmailInput.tsx:40-41`, `src/main/utils/myUser/UsernameUtils.ts:17`, `src/main/utils/EmailUtils.tsx:18` | **Medium — ✅ Fixed** | Two separate bugs in the same pattern. **(a)** The availability check (`checkUsernameExists`/`checkEmailExists`) ran inside `setTimeout(async () => {...}, 150)`, itself nested inside the outer `try { ... } catch (e) {...}` block that was meant to catch it. Because `setTimeout` schedules the callback to run *after* the synchronous `try` block has already returned, the surrounding `catch` could never actually catch a rejection from this call — any network failure became an unhandled promise rejection, and the checkmark/xmark availability indicator was left stuck in its "checking" (`null`) state forever, with no error shown to the user. **(b)** Independently, `checkUsernameExists`/`checkEmailExists` themselves returned `false` (i.e. "not taken," shown as a green checkmark) on **any** non-`ok` HTTP response — including the `429` that `API_ENDPOINTS.md` explicitly documents these two endpoints as returning at 20 requests/hour per value. A rate-limited or failed check was silently presented to the user as a confirmed-available username/email, rather than as "check failed, try again." (Registration itself would still catch a truly-taken value server-side, so this was never a security hole — just a misleading availability indicator.) **Fixed:** `checkUsernameExists`/`checkEmailExists` now `throw` (a new, translated `ERR_MSG_USERNAME_AVAILABILITY_CHECK_FAILED`/`ERR_MSG_EMAIL_AVAILABILITY_CHECK_FAILED`) instead of returning `false` on a non-`ok` response, so a failed check is distinguishable from a confirmed "not taken" result. The `setTimeout(async () => {...})` callback in both input components now has its own `try/catch` (instead of relying on the outer synchronous one, which structurally never could catch it) that sets `isAvailable` back to `null` and surfaces the thrown message via `setError`, matching the error-display pattern already used by `ConfirmPasswordInput.tsx`. Live-verified: `npx tsc --noEmit` passes; full test suite (43 suites, 464 tests) still passes with no regressions (neither component had dedicated tests exercising this specific async path before or after this fix — see §4.2, not expanded here since this was a scoped bug fix, not a coverage pass). |
| 1.4 | `src/main/components/page/Checkout/Checkout.tsx:133-152` (`handleSave`) | **Medium — ✅ Fixed** | When an authenticated user clicked "Módosítás" (Edit) on their shipping address during checkout and saved, `handleSave` only mutated the in-memory `myUserData.customer.shippingAddress.*` fields directly (lines 143-147) — it never called any backend endpoint. `Checkout.tsx`'s only `fetch()` call in the entire file was the final order submission (line 323). The edit form and "Mentés"/"Mégse" (Save/Cancel) buttons are visually identical to the real, persisting edit flow in `UserProfile/ShippingAddress.tsx` (which does `PUT /v1/customer/shipping-address`), so a user had no visual cue that this "save" was a one-order-only local override rather than an update to their stored profile. **Fixed:** `handleSave` now calls `PUT /v1/customer/shipping-address` (same endpoint, headers, and CSRF handling as `UserProfile/ShippingAddress.tsx`'s `updateShippingAddress`) before updating local state, using the existing `convertShippingAddressFormToAddressModel` converter (already used by the sibling component, so the form-data shape was already compatible) instead of the previous field-by-field mutation. On failure, the error is now shown via `handleErrorMessages`/`setModalMessage` instead of silently discarding it; the whole flow is now wrapped in `try/catch/finally`, which also incidentally closes a related edge case (the old early `if (!myUserData) return` path exited without ever resetting `setLoading(false)`, which would have left the loading overlay stuck — not separately numbered in this document, but fixed as a natural consequence of the rewrite). Live-verified: `npx tsc --noEmit` passes; full test suite passes with no regressions. Not manually re-verified against a live backend in a browser (same authentication constraint as §1.1) — the change mirrors `UserProfile/ShippingAddress.tsx`'s already-working, already-documented-correct request shape field-for-field. |
| 1.5 | `src/main/components/page/Checkout/Checkout.tsx:334`, `src/main/components/page/Register/Register.tsx:76` vs. `src/main/components/page/UserProfile/LoginData/LoginData.tsx:61`, `src/main/components/page/ForgottenPassword/ForgottenPassword.tsx:59,130` | **Medium — ✅ Fixed** | The frontend was internally inconsistent about which JSON field holds the backend's error message on a non-`ok` response: `Checkout.tsx` and `Register.tsx` read `errorData.message`; `LoginData.tsx` and both `ForgottenPassword.tsx` call sites read `errorData.error`. `API_ENDPOINTS.md` explicitly defers the authoritative error-shape contract to a backend-only doc (`docs/DESIGN.md` §9, not present in this repo) rather than documenting it inline, so which convention was actually correct couldn't be confirmed from this repo alone — but a single backend almost certainly uses one consistent field name for every error response, meaning at least one of these two groups was reading a field that doesn't exist. **Fixed:** standardized on `errorData.message` everywhere, based on the strongest available evidence — `API_ENDPOINTS.md` shows two concrete JSON response examples elsewhere in the doc (`GET /v1/password-reset/validate`'s `{ "message": "The reset link is valid!" }` and `POST /logout`'s `{ "message": "Logout successful" }`), and both use `message`, not `error`, suggesting that's the backend's general descriptive-text field name convention. `LoginData.tsx:61` and `ForgottenPassword.tsx:59,130` now read `errorData.message`, matching `Checkout.tsx`/`Register.tsx`. No test mocked `errorData.error`/`errorData.message` for these flows, so this couldn't be verified against a fixed expectation in the existing suite — flagged here for transparency rather than silently assumed correct. |
| 1.6 | `src/main/components/navigation-bar/components/logout-button/LogoutButton.tsx:36` | **Low — ✅ Fixed** | Unlike every other catch block in the app (which uses `handleErrorMessages(e)` to extract a clean message), this one did `setModalMessage(String(err))`. For the expected failure path (`throw new Error(t('nav.logoutFailed'))`), `String(err)` produced `"Error: Kijelentkezés sikertelen!"` — prepending a hardcoded, never-translated `"Error: "` in front of an otherwise fully-Hungarian-localized modal. If `fetchCsrfToken()` itself failed first, the raw untranslated English message from §7.2 below would have surfaced here too. **Fixed:** now uses `handleErrorMessages(err)`, the same shared utility every other catch block in the app uses — matches the existing convention and drops the untranslated prefix. §7.2's untranslated `fetchCsrfToken.tsx` strings can still surface here if that call fails, since fixing this call site doesn't change what `fetchCsrfToken()` itself throws — that's tracked separately under §7.2, not fixed as part of this section (out of scope for a correctness-bugs pass; §7.2 is an i18n-coverage finding). |
| 1.7 | `src/main/components/page/UserProfile/UserProfile.tsx:38-51` | **Medium, newly discovered — not yet fixed** | Discovered while writing `UserProfile.test.tsx` for §4.1 (a failure-path test that mocks a non-`ok` `GET /v1/account/me` response), not by manual code review — the existing test suite is what surfaced it. Two related bugs in the fetch-error path. **(a)** `fetchAuthenticatedUserDetails().then(setUser).catch(setModalMessage).finally(...)` passes `setModalMessage` directly as the `.catch()` handler — meaning `setModalMessage` is called with the raw rejection reason (an `Error` instance) as its argument, not `error.message`. Since `ModalContextType.setModalMessage` is typed `(message: string | null) => void`, this is a real type mismatch that TypeScript can't catch (`Promise.prototype.catch`'s handler parameter is `any`), and it briefly puts an `Error` object into `modalMessage` state, which the shared `Modal`/message-display components render directly as `{modalMessage}` — React logs `"Objects are not valid as a React child (found: [object Error])"` and aborts that render. **(b)** Separately, line 50's `if (!user) { setModalMessage(t('userProfile.fetchError')); return <></> }` calls `setModalMessage` unconditionally during render (not inside the `useEffect` or an event handler) whenever `user` is still `null` — React logs `"Cannot update a component (ModalProvider) while rendering a different component (UserProfile)"`, a documented anti-pattern (side effects don't belong in the render body). In this specific code, (b) accidentally masks (a): because (b) re-sets `modalMessage` to the correct, translated `userProfile.fetchError` string on every render where `user` is `null` — including the very next render after (a) set it to the bad `Error` object — the user ends up seeing the right final message and no *visible* symptom in production. But both console errors fire on every failed fetch (visible in dev tools, and `warnAboutRenderPhaseUpdatesInDEV`/`throwOnInvalidObjectType` are exactly the kind of noise that hides *other* real errors in the same session), and relying on one bug's render-loop side effect to overwrite another's bad state is fragile — a future refactor that removes or reorders either line would surface the raw `Error` object turning into a live "Objects are not valid as a React child" crash instead of a soft warning. **Not fixed here** — out of scope for this testing-coverage pass, following the same precedent as the already-tracked `Checkout.tsx` double-catch bug in the note below; the correct fix is `.catch((e) => setModalMessage(handleErrorMessages(e)))` (matching every other catch block's convention per §1.6) and moving line 50's `setModalMessage` call into a `useEffect` keyed on `user`, but that's a behavior change belonging to a correctness-bug pass, not this section. |

**Fixed:** 6 of 7 findings in this section addressed (all originally-identified ones). §1.7 is a new finding surfaced incidentally while adding test coverage under §4.1 — deliberately left open, consistent with this document's established practice of noting but not fixing out-of-scope bugs discovered mid-task. Live-verified together (for §1.1-1.6): `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 43 suites (464 tests); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `CI=true npm run build` exits `0`. §1.2's route/nav-link fix and its new 404 page were manually verified in a real browser (Playwright): the "Kapcsolat" link scrolls to a populated `#contact` section, and an arbitrary unmatched path renders the new not-found page correctly. §1.1's fix (from the previous PR) also lives in this section.

**Note on scope:** `AUDIT.md` §4.1 documents a `Checkout.tsx` `submitOrder` double-catch bug (a specific validation error silently overwritten by a generic one) as a real, confirmed, still-open bug. Re-verified here: still present, only line numbers shifted from `Checkout.tsx:347-358` to `Checkout.tsx:360-370` as a side effect of this section's §1.4 fix rewriting `handleSave` above it — not re-listed above to avoid duplicating that finding, and deliberately not fixed here either, since it belongs to a different, separately-tracked finding in a different document.

### 2. Security — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 2.1 | `src/main/components/page/Checkout/Checkout.tsx:330-343` (`sendOrderToServer`) | **Low — ✅ Fixed** | `fetch(isAuthenticated ? '/v1/orders' : '/v1/orders/guest', { ... headers: { 'X-CSRF-TOKEN': await fetchCsrfToken() } ... })` sent a CSRF token unconditionally for both branches. `API_ENDPOINTS.md` documents `POST /v1/orders/guest` as explicitly **CSRF-exempt** (only `POST /v1/orders`, the authenticated variant, requires it). This is the identical category of bug `AUDIT.md` §8.3 already found and fixed at three other call sites (`Login.tsx`, `ForgottenPassword.tsx` ×2) — this fourth instance, on the guest-checkout path, wasn't among them. Harmless functionally (a wasted `GET /csrf-token` round-trip before every guest order), same as the other three were. **Fixed:** `X-CSRF-TOKEN` is now only added to the request headers when `isAuthenticated` is `true`; the guest path sends only `Content-Type: application/json`, matching the documented CSRF-exempt contract exactly. Added a new test to `Checkout.test.tsx` (fills the guest form with valid data, submits, asserts the `/v1/orders/guest` fetch call's headers don't include `X-CSRF-TOKEN`) — the first test in this suite to actually exercise a full order submission. Live-verified: `npx tsc --noEmit` passes; `CI=true npm test -- --watchAll=false` passes all 43 suites (465 tests, up from 464); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `CI=true npm run build` exits `0`. Not manually re-verified in a real browser — the checkout page can't load cart contents without a live backend for `/v1/foods/cart` (unavailable in this environment), so the new Jest test (which mocks `fetch` directly) is the verification here. |
| 2.2 | `npm audit` (live, re-verified) | **High — partially fixed** | 73 vulnerabilities (0 critical, 63 high, 6 moderate, 4 low), all transitive through the unmaintained `react-scripts`/CRA build toolchain. The one full "fix everything" path (`npm audit fix --force`) resolves to installing `react-scripts@0.0.0` — not a real functional release, just npm's resolver signaling there is no version of `react-scripts` in its dependency graph that satisfies both the current tree and every advisory simultaneously; running it would delete the entire build toolchain's actual implementation (`npm start`/`build`/`test` would all stop working). **Not attempted** — this is destructive, not a fix, and matches `AUDIT.md` §2.4's own conclusion that the remaining vulnerabilities are "blocked on migrating off the unmaintained react-scripts/CRA toolchain" (a full framework migration, e.g. to Vite, is a separate, much larger undertaking than a dependency bump). **Partially fixed instead**: added `package.json`'s `overrides` field to force three specific, independently-vulnerable leaf packages deep in the dependency tree to patched versions without touching `react-scripts` itself — `underscore` (`<=1.13.7` → `^1.13.8`, unlimited-recursion DoS, `GHSA-qpx9-hpmf-5gmw`), `serialize-javascript` (`<=7.0.4` → `^7.0.7`, RCE + DoS, `GHSA-5c6j-r48x-rmvq`/`GHSA-qj8w-gfj5-8c6v`), and `uuid` (`<11.1.1` → `^14.0.1`, buffer bounds check, `GHSA-w5hq-g745-h8pq`). These are small, standalone utility libraries with minimal API surface, several levels removed from react-scripts' own webpack/babel/jest core — safely overridable without the peer-dependency conflicts that block a full `npm audit fix`. This resolved their own findings plus their dependents' derived findings (`jsonpath`/`bfj`, `css-minimizer-webpack-plugin`/`rollup-plugin-terser`, `sockjs`), taking the count from **73 to 65** (4 low, 1 moderate, 60 high — moderate dropped from 6 to 1, high from 63 to 60). Investigated the remaining ~65 individually: every one of them resolves, per `npm audit`'s own `fixAvailable` data, to either a `react-scripts@0.0.0` non-fix or a major-version bump of `eslint` (8→10, a flat-config migration for this project's still-legacy `.eslintrc.json`) or deeply interlinked `jest`/`babel`-ecosystem packages tightly version-matched to `react-scripts`' own pinned `jest@27` line — none are isolated leaf packages like the three above, so forcing them individually risks silently breaking `npm test`/`npm run lint` rather than fixing anything. Live-verified after the `overrides` change: `npx tsc --noEmit` passes; `CI=true npm test -- --watchAll=false` passes all 43 suites (465 tests); `npm run lint` exits `0` (182 problems, unchanged); `CI=true npm run build` exits `0`; `npm ci` (the exact command CI runs) resolves cleanly against the updated `package-lock.json`; the full multi-stage `docker build` (using the Dockerfile's `npm install`, not `npm ci`) also succeeds and the resulting container serves the app correctly (`curl` returned `200` with the expected page title) — verified twice, before and after this fix, for a clean before/after comparison. |
| 2.3 | `package.json` (`react-router-dom: ^6.30.0`) | **Medium — ✅ Fixed** | `react-router` was in the vulnerable `6.0.0 - 7.17.0` range (`GHSA-wrjc-x8rr-h8h6`, open redirect via backslash in `<Link>`/`useNavigate`). **Fixed:** upgraded `react-router-dom` to `7.18.1` (latest published, and the first version outside the vulnerable range). Peer requirements checked first: `react >=18`/`react-dom >=18` (already satisfied by this project's React 18.3.1) — but `engines.node >=20.0.0`, which the project's CI (`node-version: 18`) and `Dockerfile` (`FROM node:18`) both fell short of, so both were bumped to Node 20 as a required companion change (verified via a real `docker build` + container smoke test on the updated Dockerfile, not just assumption), and a `package.json` `engines` field was added to make the new hard requirement explicit (previously absent entirely — a gap `AUDIT-2.md` §5.2 had already flagged). The app's actual usage is entirely "declarative mode" routing (`BrowserRouter`/`Routes`/`Route`/`Link`/`NavLink`/`useNavigate`/`useLocation`/`useParams`/`useSearchParams`, no data routers, no loaders/actions, no RSC/framework mode) — the part of v7's API that stays closest to v6, and `tsc`/`build` both passed on the first try with zero code changes needed anywhere in `src/`. Two Jest-only breakages needed fixing (webpack/production builds were unaffected by both): (1) `react-router-dom`'s v7 package now internally requires a `./dom` subpath export via a modern package.json `exports` map, which `react-scripts@5.0.1`'s bundled Jest 27 can't resolve (`exports`-map resolution wasn't solid in Jest until v28+) — fixed with a `moduleNameMapper` entry in `package.json`'s `jest` config (a CRA-supported override key) pointing `^react-router/dom$` at the correct file directly; (2) react-router v7's code references `TextEncoder`/`TextDecoder` at module-load time, which jsdom's Jest test environment doesn't expose by default — fixed with a small polyfill at the top of `src/setupTests.ts`. **A newer, different CVE was discovered along the way** (`GHSA-qwww-vcr4-c8h2`, "RSC Mode CSRF Bypass," disclosed after the original advisory, affecting `react-router` `7.12.0 - 8.2.0` — i.e. every currently-published `react-router-dom` version above the one that fixes the original open-redirect issue). Investigated rather than assumed: (a) it's explicitly RSC/framework-mode-specific, and this app uses neither; (b) the original open-redirect issue requires attacker-controlled navigation targets, and a full audit of every `to=`/`navigate(...)` call site in `src/main` found each one is either a hardcoded literal or a backend-sourced numeric ID (`item.foodId`), never raw user input — so neither CVE has a real exploitable path in this app's actual usage, but staying on 7.18.1 (fixing the more directly-relevant original issue) was judged the better of two imperfect options over downgrading to the only version that avoids the new CVE (7.11.0, which is back inside the *original* vulnerable range). Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 43 suites (465 tests) with no code changes needed in any component; `npm run lint` exits `0` (182 problems, unchanged); `CI=true npm run build` exits `0`; manually verified in a real browser (dev server + Playwright) across all 9 routes plus the new catch-all 404 — no router-related console errors/warnings anywhere (the pre-v7 `v7_startTransition`/`v7_relativeSplatPath` future-flag warnings are gone, as expected, since v7 makes that behavior the default), `NavLink` click navigation and active-state styling both confirmed working. |
| — | `nginx.conf`, `.gitignore`, secrets scan, `dangerouslySetInnerHTML`/`eval` scan | **Clean** | Re-verified: CORS allowlist still correctly scoped (no wildcard), `.env`/`.idea/` still excluded, no hardcoded secrets/API keys found, no `dangerouslySetInnerHTML`, `eval()`, or `new Function()` anywhere in `src/main`. |

**Fixed:** all three numbered findings addressed. The user was explicitly asked whether to attempt the two breaking-dependency items given their risk, and confirmed — both were attempted with real verification (not just applied and assumed safe): §2.3's `react-router-dom` upgrade is a full, working migration with zero application code changes needed; §2.2's `npm audit` count dropped from 73 to 65 via targeted `overrides` on isolated leaf packages, with the remaining ~65 left deliberately alone since they all require either a non-functional `react-scripts@0.0.0` or a separate ESLint/Jest major-version migration — genuinely out of scope for a dependency-patching pass, not a corner cut. `npm ci`, a full Docker build + running-container smoke test, and a real-browser Playwright pass across every route all confirm nothing broke.

**Fixed:** 2.1, the one finding in this section fixable without a breaking dependency upgrade. 2.2 and 2.3 remain open by design, consistent with `AUDIT.md`'s own established treatment of the same two issues — both require breaking changes and dedicated route-by-route/full-regression testing time that a routine bug-fix pass doesn't budget for. If a breaking-change upgrade of `react-scripts` and/or `react-router-dom` is wanted, that should be a separate, explicitly-requested task.

### 3. Code quality & architecture — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 3.1 | `src/main/supports/fetch-utilities/fetchCsrfToken.tsx` (whole file) | **Low — ✅ Fixed** | Zero JSX (confirmed with a stricter check than a naive `<...>` grep, which false-positives on TypeScript generics like `Promise<string>`) but still used the `.tsx` extension — the same category `AUDIT.md` §3.7 already tracks two other instances of (`utils/EmailUtils.tsx`, all of `model/`/`builder/`). This was a *third*, independent instance, in `src/main/supports/`, a directory that was never even in §3.7's original stated scope (`converter/`, `myDecorators/`, `utils/`). **Fixed:** renamed to `fetchCsrfToken.ts` via `git mv`. No import statements needed updating, matching the pattern from the original §3.7 fix — nothing in the codebase references explicit file extensions. |
| 3.2 | `src/main/pages/Order/CustomerDetailsPage/index.tsx` | **Low — ✅ Fixed** | Exported `CheckoutPage`, imported by `App.tsx` for the `/checkout` route. The directory path (`pages/Order/CustomerDetailsPage`) matched neither the component's name (`CheckoutPage`) nor its actual purpose (the checkout form, not an order-detail or customer-detail view). This looked like a leftover from the same restructuring that left behind the dead `Order/index.tsx` `AUDIT.md` §3.4 already found and removed — this sibling survived because it's genuinely in use, just oddly placed. **Fixed:** moved to `src/main/pages/CheckoutPage/index.tsx`, matching the established sibling convention (`AllergenPage`, `MenuPage`, `LoginPage`, `RegisterPage`, `ForgottenPasswordPage`, `ShoppingCartPage`, `FoodDetailsPage`, `GalleryPage`, `NotFoundPage` — all `pages/*Page/index.tsx`). Updated the relative import path inside the moved file (one fewer directory level) and `App.tsx`'s import; the now-empty `pages/Order/` directory tree was removed. |
| 3.3 | `src/main/supports/fetch-utilities/fetchCsrfToken.ts:11` (was `fetchCsrfToken.tsx:11`, extension corrected by §3.1; this citation is now historical — the removed line no longer exists) | **Low — ✅ Fixed** | `console.error('Failed to fetch CSRF token')` immediately preceded `throw new Error('Failed to fetch CSRF token')` on the next line — the console statement added nothing the thrown error (which every caller already surfaces to the user) didn't already convey, and it was the *only* `console.error` left anywhere in `src/main` (confirmed via full-tree grep), suggesting it was a leftover from before the throw was added rather than an intentional logging strategy. **Fixed:** removed. |
| 3.4 | `src/main/components/functional/Modal/Modal.tsx:10,36` | **Informational — ✅ Fixed** | `isSuccess`/the `modal-message-success` CSS branch was dead code in current usage — grepping every `setModalMessage(...)`/`<Modal message=...>` call site in the app (30+ call sites across every page) showed all of them pass error text; genuine success confirmations go through `toast.success(...)` (react-toastify) instead, never `<Modal>`. Flagged as "not urgent to remove" at the time, but see §7.1 for why the mechanism itself was also a latent landmine, not just unused. **Fixed:** removed the `isSuccess` variable, the conditional class-name logic (now a static `className="modal-message"`), and the now-unused `.modal-message-success` CSS rule in `Modal.css`. Confirmed safe via the existing `Modal.test.tsx` suite (4 tests covering dialog semantics, focus, and `Escape`), which never exercised this branch either and passes unchanged. This also resolves §7.1, which described the exact same code. |

**Fixed:** all four findings in this section addressed, including one (3.4) that turned out to also close a separate §7 i18n finding (7.1) describing the same dead code. §7.2, in the same file touched by 3.1/3.3, remains open by design — that's an i18n-coverage gap (an untranslated string), not a code-quality issue, so translating it was left out of scope for this pass. Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 43 suites (465 tests, unchanged); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `CI=true npm run build` exits `0`. Manually verified in a real browser (dev server + Playwright): `/checkout` still renders fully and correctly after the file move (guest form, order summary, all fields present), and the existing `Modal.test.tsx` suite is the verification for the `Modal.tsx` dead-code removal, since no reachable error state in this environment (no live backend) could be found to force a fresh visual check of the modal's error styling — the removed branch was never reachable by any real call site regardless.

### 4. Testing & coverage — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 4.1 | `src/main/components/page/UserProfile/**` (`UserProfile.tsx`, `BillingAddress.tsx`, `ShippingAddress.tsx`, `PersonalData.tsx`, `LoginData.tsx`) | **High — ✅ Fixed** | Live coverage run (original): **all five files sat at 0% statements/branches/functions/lines.** This is the entire account/profile-management feature area — one of the app's larger, more state-heavy screens — with zero automated coverage of any kind. This directly explained why §1.1 (the completely unreachable Personal Data edit flow) shipped and stayed unnoticed: no test ever rendered `PersonalData` and asserted that clicking "Edit" reveals the form. `PersonalData.tsx` already got its first coverage as a side effect of the §1.1 fix (3 tests, 43.24%). **Fixed, the remaining four:** added `BillingAddress.test.tsx` and `ShippingAddress.test.tsx` (4 tests each — view mode renders the current address and an Edit button but not the form, Edit reveals the Save/Cancel form, Cancel returns to view mode, and saving calls the update callback and sends a real `X-CSRF-TOKEN` header on the `PUT` request — 85%/85% statement coverage, up from 0%); `LoginData.test.tsx` (5 tests — same view/edit/cancel pattern plus a full password-change submission asserting the CSRF header and the success path, and a case asserting the backend's exact error message surfaces on a wrong-current-password response — 95% statement coverage, up from 0%); and `UserProfile.test.tsx` (2 tests — the composed page fetches `GET /v1/account/me` and renders all four child sections with the fetched data, and a fetch failure surfaces the translated error modal message — 74.28% statement coverage, up from 0%). All four new suites build their fixtures with real `Builder` chains (`MyUserModelBuilder`/`CustomerModelBuilder`/`PersonalDetailsModelBuilder`/`AddressModelBuilder`), and `UserProfile.test.tsx` specifically round-trips a real built `MyUserModel` through `JSON.parse(JSON.stringify(...))` before handing it to the mocked `fetch`, so the test fixture matches what `res.json()` actually returns (plain deserialized data, not class instances) rather than a class instance that happens to satisfy TypeScript's structural types but wouldn't be what a real backend response produces. Writing `UserProfile.test.tsx`'s failure-path test surfaced a real, previously-undetected bug — see the new §1.7 finding below, deliberately not fixed here (same out-of-scope precedent as §4.1's own already-noted `Checkout.tsx` double-catch bug) since it's a correctness issue, not a coverage gap. |
| 4.2 | `src/main/components/input/myUser/UsernameInput/UsernameInput.tsx`, `EmailInput.tsx` | **Medium — ✅ Fixed** | 45.45% / 46.66% statement coverage respectively; the live coverage report's uncovered-line ranges (`UsernameInput.tsx:35-54,86-109`; `EmailInput.tsx:37-56,87-111`) directly contained the exact async availability-check block described in §1.3 — the buggy code path was untested. **Fixed:** added `UsernameInput.test.tsx` and `EmailInput.test.tsx` (3 tests each — shows an available checkmark when the value is valid and not taken, shows an unavailable mark and error when it's taken, and a dedicated regression test for the §1.3 fail-open/unhandled-rejection bug: a non-`ok` availability-check response now surfaces the translated "check failed" error text and shows no false "available" checkmark). Now 68.08% / 68.75% statement coverage — the previously-uncovered async block, including the specific §1.3 fix, is now exercised directly. |
| 4.3 | `src/main/supports/fetch-utilities/fetchCsrfToken.ts` (was `fetchCsrfToken.tsx` — extension corrected by §3.1; this citation is now historical) | **Low — ✅ Fixed** | 0% coverage — the shared CSRF-token utility used by 7 different mutating call sites across the app had no test of its own, including no test of its error path (§2.1/§3.3/§7.2). **Fixed:** added `fetchCsrfToken.test.ts` (2 tests — requests `/csrf-token` with `credentials: 'include'` and returns the parsed token, and throws when the response is not `ok`). Now 100% statement/branch/function/line coverage. |
| — | `package.json`'s `jest.coverageThreshold` (45/30/43/46) vs. live coverage (47.34/31.58/44.51/48.36) | **Informational — ✅ Addressed** | Live coverage grew substantially from this section's fixes (47.34/31.58/44.51/48.36 → **61.1/45.55/56.81/61.94**, see the Appendix). Per this document's own note that the floor is "a regression gate set a few points below current actual coverage, not an aspirational target," it's been re-tightened to `59/43/54/59` — closing most of the gap that had opened up since the original floor was set, while keeping the same small buffer below live numbers `AUDIT.md` §4.2 originally established. |

**Fixed:** all three numbered findings addressed — every file in this section's original scope now has real coverage, and the coverage-threshold floor has been re-tightened to match. Live-verified together: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 50 suites (488 tests, up from 43/465 — 7 new suites: `fetchCsrfToken.test.ts`, `UsernameInput.test.tsx`, `EmailInput.test.tsx`, `BillingAddress.test.tsx`, `ShippingAddress.test.tsx`, `LoginData.test.tsx`, `UserProfile.test.tsx`); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `npm run test:coverage` passes the new, tightened `59/43/54/59` threshold at live `61.1/45.55/56.81/61.94`; `CI=true npm run build` exits `0`. Not manually re-verified in a real browser — every change in this section is a new `src/test/**` file with zero `src/main/**` behavior changes (aside from the coverage-threshold number in `package.json`, which has no runtime effect), so the automated suites are the verification here.

### 5. Tooling & CI/CD — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 5.1 | repo-wide | **Low — ✅ Fixed** | No Dependabot/Renovate config (checked `.github/` — only `workflows/` exists, no `dependabot.yml`). Given §2.2's 65 open `npm audit` findings, there was no automated mechanism to surface *newly*-disclosed vulnerabilities going forward — someone had to remember to re-run `npm audit` manually, same as how §2.3 (`react-router`) was originally discovered mid-audit rather than flagged proactively. **Fixed:** added `.github/dependabot.yml` with three `package-ecosystem` entries, each on a weekly schedule — `npm` (the app's actual dependency tree, `open-pull-requests-limit: 10` so a backlog doesn't silently cap future PRs), `github-actions` (`.github/workflows/ci.yml`'s own action versions), and `docker` (the `Dockerfile`'s `node:20`/`nginx:stable-alpine` base images) — covering every dependency surface this repo actually has. GitHub's Dependabot doesn't require any repo-side enablement beyond this file being present and Dependabot being on for the repository (a GitHub-side setting, not something committable) — no workflow or CI change was needed for the file itself to take effect. Verified as valid YAML (parsed cleanly) and matches the schema GitHub's own `dependabot.yml` documentation describes; no `src/`/`package.json` changes, so no test/build impact. |
| 5.2 | `package.json` | **Low — ✅ Fixed** | No `engines` field, despite `Dockerfile` (`FROM node:18`) and `.github/workflows/ci.yml` (`node-version: 18`) both implicitly targeting Node 18 specifically. A contributor running `npm install`/`npm start` on a different major Node version locally (this sandbox's own Node is v24) gets no warning of a potential mismatch — relevant given `react-scripts`/CRA 5's known sensitivity to Node version differences. **Fixed as a side effect of §2.3**: upgrading `react-router-dom` to 7.18.1 introduced a real `engines.node >=20.0.0` requirement, so `Dockerfile` and `.github/workflows/ci.yml` were both bumped from Node 18 to Node 20 (verified via an actual `docker build` + running-container smoke test, not just the version bump alone), and `package.json` now has `"engines": { "node": ">=20" }` making the requirement explicit for anyone installing locally — closing this exact gap, now for a genuinely load-bearing reason rather than just as documentation. |

**Fixed:** both findings in this section addressed — 5.2 as a side effect of the §2.3 dependency upgrade, 5.1 (the last open item) via a new `.github/dependabot.yml`. Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 50 suites (488 tests, unchanged); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `CI=true npm run build` exits `0`. No manual browser verification needed — this section's only remaining change is a new GitHub-side config file with no `src/`/runtime effect.

### 6. Accessibility — ✅ Fixed

| # | Location | Severity | Description |
|---|---|---|---|
| 6.1 | `public/index.html:2` | **High — ✅ Fixed** | `<html lang="en">`, but every single piece of user-facing text in the app is Hungarian (confirmed: the app has exactly one configured locale, `hu`, per `src/main/i18n/i18n.ts`). Screen readers use the document's `lang` attribute to select pronunciation rules — with this mismatch, assistive technology will attempt to read 100% of the app's Hungarian content using English phonetics, producing largely incomprehensible speech output for blind/low-vision users. This file is static HTML outside the React tree, so it wasn't touched by `AUDIT.md` §7's `i18next` migration (which only covered `src/main/**`) and has apparently never been revisited since. **Fixed:** changed to `<html lang="hu">`, a one-line, one-word change with no other effect on the file. Verified in a real browser (dev server + Playwright): `document.documentElement.lang` reads `"hu"` at runtime, and the production `build/index.html` output has the same attribute — confirming CRA's build step doesn't strip or override it. |
| 6.2 | `src/main/components/header/Header.tsx:20`, all `page-title`-classed headings app-wide | **Low — ✅ Fixed** | No `<h1>` exists anywhere in the persistent app shell — the "ImagineBar" brand/logo (`Header.tsx:20`) is a bare `<span>`, not a heading — and most individual pages start their heading hierarchy at `<h2 className='page-title'>` (`Menu`, `Allergen`, `FoodDetails`, `UserProfile`, etc.). Exactly two conditional states break this pattern by using `<h1 className='page-title'>` for the identical visual role instead: `Checkout.tsx:415` (order-success message) and `ShoppingCart.tsx:67` (empty-cart message). Net effect: most pages have no `<h1>` at all, and the two that do use it inconsistently for what's everywhere else treated as an `<h2>`-level role — unpredictable landmark structure for screen-reader users navigating by heading level. Not caught by `eslint-plugin-jsx-a11y`, which doesn't enforce heading-level consistency. **Fixed:** every `page-title`-classed heading that represents a page's own singular title is now `<h1>` — `NotFound.tsx`, `Allergen.tsx`, `UserProfile.tsx`, `FoodDetails.tsx` (all `h2`→`h1`), plus `Checkout.tsx:395` and `ShoppingCart.tsx:71` (the other halves of the two state-toggling pairs 6.2 originally described, previously `h2` while their sibling state was already `h1`) — giving every page exactly one real `<h1>` where it previously had none, and resolving the toggle-inconsistency directly. `.page-title`'s CSS (`src/main/css/page-title.css`) selects purely by class, not tag, so this changes semantics only, not layout — confirmed identical visual rendering in a real browser (dev server + Playwright screenshots) on `NotFoundPage` and the empty-cart state of `ShoppingCart`, the two states reachable without a live backend in this environment. **Deliberately left untouched, out of scope:** `Menu.tsx:97`'s `<h2 className='page-title'>` and `home/index.tsx:87,109`'s `<h3 className='page-title'>` are a structurally different pattern — repeated per-category/per-section subheadings with no single page-level title above them, not a page toggling between h1 and h2 for the same role — so converting them to `<h1>` would just trade one heading-outline problem (no h1) for another (multiple h1s per page, or a still-orphaned h3 with nothing above it). Fixing that properly means adding a real page-level heading to `Menu`/`Home` that doesn't exist today, which is a larger, separate change than the specific h1/h2 inconsistency this finding described. |

**Fixed:** both findings in this section addressed. Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 50 suites (488 tests, unchanged — no test asserted a specific heading level); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `CI=true npm run build` exits `0`, and the built `build/index.html` was checked directly for the `lang="hu"` attribute. Manually verified in a real browser (dev server + Playwright): `NotFoundPage` (`/nonexistent-path`) and `ShoppingCart`'s empty-cart state (`/cart`) — the two `page-title` sites reachable without a live backend in this environment — both render their heading text correctly and visually unchanged after the tag swap. `Allergen`, `UserProfile`, `FoodDetails`, and `Checkout`'s data-entry state all gate their `page-title` heading behind a backend fetch that hangs indefinitely without a live backend (same constraint noted in earlier sections of this document), so those four couldn't be re-verified visually in this pass — the tag-only, CSS-unaffected nature of the change (confirmed on the two reachable pages) is the basis for treating them as low-risk.

### 7. Internationalization (i18n) — ✅ Fixed (7.3 informational, confirmed unchanged by design)

`AUDIT.md` §7 already did a full `i18next` extraction across `src/main/**`; re-verified that migration's own scope is intact (fresh `grep` for Hungarian-accented characters outside `i18n/locales/` still turns up only regex character classes and the `Footer.tsx` proper-noun, as documented). The gaps below are things that migration's stated scope (`src/main/**`) structurally couldn't reach, or that its own line-by-line sweep missed.

| # | Location | Severity | Description |
|---|---|---|---|
| — | See §6.1 | **High — ✅ Fixed** | `public/index.html`'s `<html lang="en">` — an i18n gap as much as an accessibility one; cross-listed rather than duplicated. Fixed as part of §6.1 (now `lang="hu"`). |
| 7.1 | `src/main/components/functional/Modal/Modal.tsx:10` | **Low — ✅ Fixed** | `const isSuccess = message.toLowerCase().includes('siker')` — success/error styling was determined by substring-matching the literal Hungarian word "siker" (from "sikeres", success) directly in component logic, entirely bypassing the `i18next` architecture the rest of the app now uses. This wasn't hardcoded *display* text (so `AUDIT.md` §7's string-extraction sweep wouldn't have flagged it), but it was the same underlying problem: locale-specific assumptions baked into shared logic. It would have silently and permanently stopped working — no message would ever render as "success" styled — the moment a second locale is added, with no error or warning. **Fixed as part of §3.4**, which independently identified this same code as dead (nothing ever called `<Modal>` with success text) and removed it entirely — the hidden locale-coupling landmine is gone along with the dead branch it lived in. |
| 7.2 | `src/main/supports/fetch-utilities/fetchCsrfToken.ts:11` (was `fetchCsrfToken.tsx:11-12` — extension corrected by §3.1, and the redundant `console.error` on the line immediately above this one was removed entirely by §3.3, so only the single `throw` remains) | **Medium — ✅ Fixed** | One hardcoded, untranslated English string: `throw new Error('Failed to fetch CSRF token')`. Unlike the `OrderModel`-style internal validation messages (§7.3, below — effectively unreachable), this one is genuinely reachable by real users: any network hiccup while fetching a CSRF token (used by 7 different mutating flows — checkout, registration, profile edits, logout) throws this, and `ErrorUtils.tsx`'s `handleErrorMessages()` surfaces a plain `Error`'s `.message` verbatim, so this literal English string can appear directly in an otherwise fully-Hungarian modal. Given the user explicitly chose "full extraction, everything" for `AUDIT.md` §7, this was a genuine (if narrow) miss — `fetch-utilities/` isn't `utils/**`, `converter/**`, or component JSX, so it fell outside every category that migration's file-by-file sweep covered. **Fixed:** added a new `errors.ERR_MSG_CSRF_TOKEN_FETCH_FAILED` key to `hu.json` ("Hiba történt a biztonsági token lekérése során, próbálja újra!", matching the existing "Hiba történt X során, próbálja újra!" phrasing already used by `ERR_MSG_USERNAME_AVAILABILITY_CHECK_FAILED`/`ERR_MSG_EMAIL_AVAILABILITY_CHECK_FAILED`), and `fetchCsrfToken.ts` now imports the `i18n` singleton and throws a module-level `ERR_MSG_CSRF_TOKEN_FETCH_FAILED` constant instead of the literal string — the same free-function-outside-a-component pattern documented in `CLAUDE.md` (`i18n.t(...)` called directly, not the `useTranslation()` hook, since this file has no component tree) and already used by `UsernameUtils.ts`/`EmailUtils.tsx`'s own `*_AVAILABILITY_CHECK_FAILED` constants. `fetchCsrfToken.test.ts` (added in the §4.3 testing pass) updated to assert the new Hungarian string; still 100% statement/branch/function/line coverage. |
| 7.3 | `src/main/model/OrderModel.tsx:8,9,12,13,16,17,20,21`, `src/main/model/order/OrderItemModel.tsx:6,7,10,11`, `src/main/model/food/AllergenModel.tsx:17` | **Informational, confirmed unchanged** | Hardcoded English `class-validator` messages (e.g. `'Customer email is required (null)'`) remain untranslated. Re-confirmed this matches the `i18n` migration's own explicit, documented scope decision (English content was deliberately left alone) rather than an oversight — these are defensive internal-invariant checks on objects the app always constructs validly itself, not reachable through normal user action. Listed here only for completeness, not as a new finding. |

**Fixed:** both open findings in this section addressed (7.2 here; the cross-listed §6.1 item and 7.1 were already fixed in earlier PRs). 7.3 remains open by design — an explicit, documented scope decision from the original `i18next` migration, not a defect. Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 50 suites (488 tests, unchanged); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `npm run test:coverage` still reports `fetchCsrfToken.ts` at 100% statement/branch/function/line coverage; `CI=true npm run build` exits `0`. Not manually re-verified in a real browser — this is a pure error-string swap with no visual/DOM-structure change, and the updated `fetchCsrfToken.test.ts` asserts the exact new Hungarian string directly, which is stronger evidence for this specific change than a browser click-through (reproducing the failure condition — a live backend rejecting `GET /csrf-token` — isn't reachable in this environment anyway, same constraint noted throughout this document).

### 8. Backend API contract — ✅ Fixed

Re-cross-referenced every `fetch()` call site in `src/main` against `API_ENDPOINTS.md` from scratch. All 15 previously-mismatched paths/methods `AUDIT.md` §8.1 fixed remain correctly fixed — no regressions on path or method anywhere. Spot-checked request-body shapes for `POST /v1/registration`, `PATCH /v1/account/password`, `PUT /v1/customer/*-address`, and `POST /v1/orders`/`/v1/orders/guest` against the documented JSON shapes — all match field-for-field, with one exception:

| # | Location | Severity | Description |
|---|---|---|---|
| 8.1 | `src/main/model/OrderModel.tsx:22,28,33` | **Low — ✅ Fixed** | `OrderModel` has a fourth constructor parameter, `isAuthenticatedUser?: boolean`, which becomes a real field in the object `JSON.stringify`'d and sent as the request body for both `POST /v1/orders` and `POST /v1/orders/guest`. `API_ENDPOINTS.md`'s documented request body for both endpoints is exactly `{ customer, orderItems, paymentType }` — no fourth field. Whether a Spring/Jackson backend errors or silently ignores an unrecognized JSON property depends on its `@JsonIgnoreProperties` configuration (not visible from this repo) — likely harmless under Jackson's common default of ignoring unknown properties, but it's a genuine, easily-fixed contract mismatch: the URL path itself (`/v1/orders` vs. `/v1/orders/guest`) already fully encodes the authentication state the backend needs, making this field redundant even if it is silently accepted. **Fixed:** removed `isAuthenticatedUser` entirely from `OrderModel` — the field, its `@NotNull`/`@NotUndefined` decorators, and the constructor parameter — so the class now has exactly the three documented fields (`customer`, `orderItems`, `paymentType`) and nothing else can leak into the serialized body. `Checkout.tsx`'s `new OrderModel(getCustomer(), orderItems, paymentType, isAuthenticated)` call site (the only caller) updated to drop the now-removed fourth argument; `isAuthenticated` itself is untouched and still solely drives the `/v1/orders` vs. `/v1/orders/guest` URL choice, exactly as before. Added a new `Checkout.test.tsx` test (`'sends an order request body matching API_ENDPOINTS.md exactly (no extra fields)'`) that submits a full guest order and asserts `Object.keys(JSON.parse(body)).sort()` equals exactly `['customer', 'orderItems', 'paymentType']` — a real regression test, not just a shape-matches-by-inspection claim. No test previously existed for `OrderModel` or asserted on the order request body shape at all, so this is also this class's first-ever test coverage of any kind. |
| — | See §2.1 | **Low — ✅ Fixed** | The `/v1/orders/guest` CSRF-exempt violation — cross-listed under Security rather than duplicated; already fixed in an earlier PR. |

**Fixed:** the one addressable finding in this section (8.1) is resolved. Live-verified: `npx tsc --noEmit` passes with zero errors; `CI=true npm test -- --watchAll=false` passes all 50 suites (489 tests, up from 488 — the new order-body-shape test); `npm run lint` exits `0` with 182 problems (0 errors, unchanged); `npm run test:coverage` unchanged at 61.1/45.55/56.81/61.94, still passing the threshold; `CI=true npm run build` exits `0`. Not manually re-verified in a real browser — this changes only the JSON payload sent over the wire, with no DOM/visual effect and no live backend available in this environment to observe the real request against (same constraint noted throughout this document); the new Jest test, which asserts on the actual serialized body Checkout produces, is the verification here.

**Still open, confirmed unchanged from `AUDIT.md` §8.5:** `POST /v1/foods`, `GET /v1/customers`, `GET /v1/orders/{id}`, `GET /v1/ingredients` remain documented but uncalled — no admin UI, order-history, or ingredient-listing feature exists yet. Not a defect.

---

## Summary table

| # | Finding | Severity |
|---|---|---|
| 1.1 | ~~`UserProfile`'s Personal Data edit flow is completely unreachable (inverted `editMode` condition)~~ | ~~High~~ **✅ Fixed** — button condition corrected, first-ever test coverage added |
| 1.2 | ~~"Kapcsolat" nav link points at a route that doesn't exist — blank page, no 404 handling~~ | ~~High~~ **✅ Fixed** — link now scrolls to Footer's contact section; added a real catch-all 404 page |
| 6.1 | ~~`<html lang="en">` on an all-Hungarian app — screen readers mispronounce everything~~ | ~~High~~ **✅ Fixed** — changed to `lang="hu"`, verified in a real browser and in the production build output |
| 1.7 | `UserProfile.tsx`'s fetch-error path: raw `Error` object passed to `setModalMessage`, plus a render-phase `setModalMessage` call — newly discovered while fixing 4.1 | **Medium, not yet fixed** |
| 2.2 | ~~73 open `npm audit` vulnerabilities (0 critical) — unchanged, no further non-breaking fix~~ | ~~High~~ **Partially fixed** — 73→65 via targeted `overrides` on isolated leaf packages; rest genuinely blocked on `react-scripts@0.0.0`/ESLint major bump |
| 1.3 | ~~Username/email availability checks: unhandled-rejection path + fail-open on rate-limit/errors~~ | ~~Medium~~ **✅ Fixed** — failed checks now throw and are caught, no longer shown as "available" |
| 1.4 | ~~Checkout's "edit shipping address" never persists to the backend, looks like it does~~ | ~~Medium~~ **✅ Fixed** — now calls the same `PUT` endpoint the profile page uses |
| 1.5 | ~~Frontend reads inconsistent error-response field names (`error` vs `message`) across 4 call sites~~ | ~~Medium~~ **✅ Fixed** — standardized on `errorData.message` |
| 2.3 | ~~`react-router` open-redirect advisory, needs a major bump — unchanged, not attempted~~ | ~~Medium~~ **✅ Fixed** — upgraded to 7.18.1, zero app code changes, full browser verification |
| 4.1 | ~~`UserProfile` + all 4 subsections at 0% test coverage — directly enabled 1.1 shipping unnoticed~~ | ~~High~~ **✅ Fixed** — all 5 files now covered (43-95% statements) |
| 4.2 | ~~`UsernameInput`/`EmailInput` ~45% covered; the buggy async block is exactly what's uncovered~~ | ~~Medium~~ **✅ Fixed** — now ~68% each, including a regression test for the §1.3 fix |
| 7.2 | ~~`fetchCsrfToken.tsx`'s hardcoded English string, reachable by real users~~ | ~~Medium~~ **✅ Fixed** — now translated via a new `ERR_MSG_CSRF_TOKEN_FETCH_FAILED` key |
| 1.6 | ~~`LogoutButton.tsx` shows raw `String(err)` with an untranslated "Error:" prefix~~ | ~~Low~~ **✅ Fixed** — now uses `handleErrorMessages`, matching every other catch block |
| 2.1 | ~~Guest checkout still sends CSRF token to a documented CSRF-exempt endpoint~~ | ~~Low~~ **✅ Fixed** — token now only attached for authenticated orders |
| 3.1 | ~~`fetchCsrfToken.tsx`: `.tsx` extension, zero JSX — third instance of the §3.7-category gap~~ | ~~Low~~ **✅ Fixed** — renamed to `.ts` |
| 3.2 | ~~`pages/Order/CustomerDetailsPage/index.tsx` exports `CheckoutPage` — naming mismatch~~ | ~~Low~~ **✅ Fixed** — moved to `pages/CheckoutPage/index.tsx` |
| 3.3 | ~~Leftover `console.error` duplicating the very next line's thrown error~~ | ~~Low~~ **✅ Fixed** — removed |
| 4.3 | ~~`fetchCsrfToken.tsx` at 0% coverage~~ | ~~Low~~ **✅ Fixed** — now 100% covered, including the error path |
| 5.1 | ~~No Dependabot/Renovate — 65 known vulnerabilities rely on manual re-audits~~ | ~~Low~~ **✅ Fixed** — added `.github/dependabot.yml` (npm/github-actions/docker, weekly) |
| 5.2 | ~~No `engines` field in `package.json` despite Docker/CI both pinning Node 18~~ | ~~Low~~ **✅ Fixed** — Docker/CI bumped to Node 20 for react-router-dom 7.x, `engines` field added |
| 6.2 | ~~No consistent `<h1>` per page; 2 of ~10 pages inconsistently use it for the same role~~ | ~~Low~~ **✅ Fixed** — every singular page-title heading is now `<h1>`; `Menu`/`Home`'s structurally different per-section subheadings left as-is |
| 7.1 | ~~`Modal.tsx`'s success/error styling hinges on matching the literal string "siker"~~ | ~~Low~~ **✅ Fixed** — dead code removed as part of §3.4 |
| 8.1 | ~~`OrderModel` sends an undocumented `isAuthenticatedUser` field in the order request body~~ | ~~Low~~ **✅ Fixed** — field removed entirely; body now matches `API_ENDPOINTS.md` exactly |
| 3.4 | ~~`Modal.tsx`'s success-styling branch is dead code in current usage~~ | ~~Informational~~ **✅ Fixed** — removed; also resolved §7.1 |
| 7.3 | `OrderModel`/`OrderItemModel`/`AllergenModel`'s internal English validator messages | **Informational, confirmed unchanged** |
| 8.2 | 4 documented backend endpoints still have no frontend caller | **Informational, confirmed unchanged** |

---

## Appendix: live tool output (this pass, 2026-07-27)

```
$ npx tsc --noEmit
(no output, exit code 0)

$ npm run lint
✖ 182 problems (0 errors, 182 warnings)   # unchanged from AUDIT.md's documented state;
                                            # all warnings are pre-existing src/test/** no-explicit-any
(process exit code: 0)

$ CI=true npm test -- --watchAll=false
Test Suites: 42 passed, 42 total
Tests:       461 passed, 461 total
(process exit code: 0 — unchanged)

$ npm run test:coverage
All files | 47.34% Stmts | 31.58% Branch | 44.51% Funcs | 48.36% Lines
(process exit code: 0 — passes the 45/30/43/46 threshold; see §4)

$ CI=true npm run build
Compiled successfully.
(process exit code: 0 — unchanged)

$ npm audit
73 vulnerabilities (4 low, 6 moderate, 63 high, 0 critical)
(unchanged from AUDIT.md §2.4/§2.6's documented state; react-router still in the
 vulnerable 6.0.0-7.17.0 range per §2.3 above)
```

No regressions found anywhere in this baseline — every number above matches what `AUDIT.md` already documents as the current, fixed state. All findings in this document are genuinely new, independently discovered by reading current source rather than by re-running the same checks `AUDIT.md` already ran.

```
$ npx tsc --noEmit   # after the §1 fixes (all six findings, 1.1-1.6)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §1 fixes
Test Suites: 43 passed, 43 total
Tests:       465 passed, 465 total
(process exit code: 0; 42→43 suites and 461→465 tests is PersonalData.test.tsx,
 new for §1.1, plus the new Checkout.test.tsx CSRF-assertion test added for §2.1)

$ npm run lint   # after the §1 fixes
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ CI=true npm run build   # after the §1 fixes
Compiled successfully.
(process exit code: 0)

$ npx tsc --noEmit   # after the §2.2/§2.3 breaking-dependency upgrades, done with
                      # explicit user authorization
(no output, exit code 0 — zero application code changes needed for the
 react-router-dom 6->7 migration)

$ CI=true npm test -- --watchAll=false   # after §2.2/§2.3
Test Suites: 43 passed, 43 total
Tests:       465 passed, 465 total
(process exit code: 0 — unchanged from immediately before the upgrade; two
 Jest-only fixes were needed to get here: a moduleNameMapper entry for
 react-router's ./dom subpath export, and a TextEncoder/TextDecoder polyfill
 in setupTests.ts — see §2.3)

$ npm run lint   # after §2.2/§2.3
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ CI=true npm run build   # after §2.2/§2.3
Compiled successfully.
(process exit code: 0; bundle +6.82 kB gzipped from the react-router-dom major bump)

$ npm ci   # the exact command CI runs, re-verified against the final package-lock.json
added 1491 packages, and audited 1492 packages in 11s
65 vulnerabilities (4 low, 1 moderate, 60 high, 0 critical)
(down from 73 — see §2.2; resolves cleanly, no peer-dependency errors)

$ docker build -t reference-react-typescript-test .   # full multi-stage build,
                                                          # using the Dockerfile's
                                                          # own `npm install` (not `npm ci`)
Compiled successfully.
... naming to docker.io/library/reference-react-typescript-test:latest done
(exit code 0; run twice — once immediately after the Node 18->20 Dockerfile bump
 with react-router-dom already upgraded, once again after the §2.2 overrides
 were added — both succeeded identically)

$ docker run -d -p 8081:80 reference-react-typescript-test && curl -s -o /dev/null -w "%{http_code}" http://localhost:8081/
200
(the built container actually serves the app correctly, not just "builds without
 erroring" — confirmed via a real HTTP request against the running nginx container,
 and the response body's <title> tag matched the expected page title)

$ npx tsc --noEmit   # after the §3 code-quality fixes (3.1-3.4)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §3 fixes
Test Suites: 43 passed, 43 total
Tests:       465 passed, 465 total
(process exit code: 0 — unchanged; Modal.test.tsx's 4 tests pass unchanged too,
 confirming the dead isSuccess branch removed in 3.4 was never exercised)

$ npm run lint   # after the §3 fixes
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ CI=true npm run build   # after the §3 fixes
Compiled successfully.
(process exit code: 0)

$ npx tsc --noEmit   # after the §4 test-coverage fixes (4.1-4.3)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §4 fixes
Test Suites: 50 passed, 50 total
Tests:       488 passed, 488 total
(process exit code: 0; 43->50 suites and 465->488 tests is 7 new suites:
 fetchCsrfToken.test.ts, UsernameInput.test.tsx, EmailInput.test.tsx,
 BillingAddress.test.tsx, ShippingAddress.test.tsx, LoginData.test.tsx,
 UserProfile.test.tsx)

$ npm run lint   # after the §4 fixes
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ npm run test:coverage   # after the §4 fixes, against the re-tightened threshold
All files | 61.1% Stmts | 45.55% Branch | 56.81% Funcs | 61.94% Lines
(process exit code: 0 — passes the new 59/43/54/59 threshold; up from
 47.34/31.58/44.51/48.36 against the old 45/30/43/46 threshold; see §4)

$ CI=true npm run build   # after the §4 fixes
Compiled successfully.
(process exit code: 0)

$ npx tsc --noEmit   # after the §5.1 fix (.github/dependabot.yml)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §5.1 fix
Test Suites: 50 passed, 50 total
Tests:       488 passed, 488 total
(process exit code: 0 — unchanged; this fix touched no src/ files)

$ npm run lint   # after the §5.1 fix
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ CI=true npm run build   # after the §5.1 fix
Compiled successfully.
(process exit code: 0)

$ npx tsc --noEmit   # after the §6 accessibility fixes (6.1-6.2)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §6 fixes
Test Suites: 50 passed, 50 total
Tests:       488 passed, 488 total
(process exit code: 0 — unchanged; no test asserted a specific heading level)

$ npm run lint   # after the §6 fixes
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ CI=true npm run build   # after the §6 fixes
Compiled successfully.
(process exit code: 0; build/index.html confirmed to contain <html lang="hu">)

$ npx tsc --noEmit   # after the §7.2 fix (fetchCsrfToken.ts translation)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §7.2 fix
Test Suites: 50 passed, 50 total
Tests:       488 passed, 488 total
(process exit code: 0 — unchanged; fetchCsrfToken.test.ts updated in place to
 assert the new Hungarian string rather than adding a new test)

$ npm run lint   # after the §7.2 fix
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ npm run test:coverage   # after the §7.2 fix
fetchCsrfToken.ts | 100% Stmts | 100% Branch | 100% Funcs | 100% Lines   # unchanged

$ CI=true npm run build   # after the §7.2 fix
Compiled successfully.
(process exit code: 0)

$ npx tsc --noEmit   # after the §8.1 fix (OrderModel.isAuthenticatedUser removed)
(no output, exit code 0)

$ CI=true npm test -- --watchAll=false   # after the §8.1 fix
Test Suites: 50 passed, 50 total
Tests:       489 passed, 489 total
(process exit code: 0; 488->489 tests is the new Checkout.test.tsx test
 asserting the order body's exact key set)

$ npm run lint   # after the §8.1 fix
✖ 182 problems (0 errors, 182 warnings)   # unchanged
(process exit code: 0)

$ npm run test:coverage   # after the §8.1 fix
All files | 61.1% Stmts | 45.55% Branch | 56.81% Funcs | 61.94% Lines   # unchanged, still passes threshold

$ CI=true npm run build   # after the §8.1 fix
Compiled successfully.
(process exit code: 0)
```
