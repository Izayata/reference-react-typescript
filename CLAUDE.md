# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

A Create React App (react-scripts 5) + TypeScript single-page app for a restaurant ordering site (menu browsing, food details, allergens, gallery, shopping cart, checkout, registration/login, account/profile management). Bootstrapped from CRA's Redux+TypeScript template (`package.json` name is still `redux-typescript-router`, and `src/main/features/counter` is the original template's demo Redux slice, largely unused by app logic) and then heavily extended into the restaurant domain.

The app talks to a separate backend (proxied at `localhost:8080` in dev via the `proxy` field in `package.json`; in the Docker/nginx setup, `nginx.conf` proxies `/auth-status`, `/csrf-token`, `/login`, `/v1`, `/actuator` to a service named `app:8080` — the `/actuator` path suggests a Spring Boot backend). Auth is cookie/session-based (`credentials: 'include'`) with CSRF token fetching (`src/main/supports/fetch-utilities/fetchCsrfToken.ts`).

## Known issues — check `AUDIT.md`, `AUDIT-2.md`, `AUDIT-3.md`, and `AUDIT-4.md` before assuming existing code is correct

`AUDIT.md` is a living audit of this codebase (security, correctness bugs, test coverage, tooling/CI, accessibility, and a full frontend/backend API cross-check against `API_ENDPOINTS.md`). `AUDIT-2.md` is a second, independent audit pass covering the same categories plus internationalization — every finding in it was re-derived from reading current source, not copied from `AUDIT.md`'s conclusions. `AUDIT-3.md` and `AUDIT-4.md` are a third and fourth independent pass, each following the same rule (findings re-derived from current source, cross-referenced against every prior pass before being reported so nothing is duplicated). For deeper architectural context than these audit narratives provide, see `docs/DESIGN.md`. Check all four before assuming an issue is still open or that existing code is correct, and update the relevant finding in whichever document describes it (mark it fixed, correct a stale line citation, etc.) when you fix or materially change something any of them describes.

Most of the originally-found issues in `AUDIT.md` are now fixed: the three silent-error-swallowing bugs, the empty `App.test.tsx` suite, all 15 `fetch()` calls that used a URL path not matching `API_ENDPOINTS.md` (§8.1/§8.2 — endpoint paths now match the doc, including `Register.tsx`'s signup call), the `nginx.conf` CORS wildcard, `AccountRouteGuard`'s dead state, and `/logout`'s CSRF transport. §8.3 is also fixed: `Login.tsx` and `ForgottenPassword.tsx`'s three calls to `/login`, `/v1/password-reset/request-password-reset-link`, and `/v1/password-reset/set-new-password` — all three documented **CSRF-exempt** — no longer fetch or attach a CSRF token, so those requests now match `API_ENDPOINTS.md`'s documented bodies exactly instead of carrying an unnecessary `_csrf`/`X-CSRF-TOKEN`. With that, **§8 (backend API contract mismatches) is fully addressed** — only its informational §8.5 note (four documented backend endpoints with no frontend feature built yet — admin UI, order history, etc.) remains, by design, not a defect. `npm audit`'s 2 critical vulnerabilities are also fixed (§2.4) — but that fix (a routine `npm audit fix`) surfaced a new, separate finding: `react-router` itself (a real runtime dependency, not build tooling) had a moderate open-redirect advisory with no non-breaking fix available at the time (§2.6) — since fixed in `AUDIT-2.md`'s pass, see below. All §3 code-quality/architecture findings are also fixed: no more `any` at the form/error boundary, the dead `counter` feature and stray `console.log`s are gone, `AddressModelConverter` is de-duplicated, non-JSX files use `.ts` not `.tsx`, and `Checkout.tsx` is split into an orchestrator plus two presentational sub-components (see "Directory layout" and the Checkout section above). All §4 testing/coverage findings are also fixed: `Login`, `Register`, `Checkout`, `ShoppingCart`, and `AccountRouteGuard` now each have a test suite under `src/test/components/**`, and `package.json` has a `jest.coverageThreshold` floor plus a `test:coverage` script so coverage regressions would now be caught. That pass surfaced (but deliberately did not fix, being out of scope for a testing task) a real bug: `Checkout.tsx`'s `submitOrder` has a double-catch that silently overwrites a specific validation error message with a generic one — see §4.1; `AUDIT-2.md` re-confirmed this is still present, and it remains the one correctness bug open across both documents. All §5 tooling/CI-CD findings are also fixed: `npm run lint` went from 126 errors to 0 (115 auto-fixed, 11 manual `require()`→`import` rewrites), the 44 `src/main/**` warnings that were failing `CI=true npm run build` are gone (mostly dead imports — one, `NavigationBar.tsx`'s `nav.module.scss`, pointed at a file that never existed anywhere in the repo's history and would have thrown a real webpack error once the lint-error gate stopped masking it), `.github/workflows/ci.yml` runs lint/typecheck/test+coverage/build on every push/PR, a `husky`+`lint-staged` pre-commit hook runs `eslint` on staged files, `eslint-plugin-jsx-a11y` is now active (see "Linting conventions" below), and `tsconfig.json`'s `target` now matches `browserslist` (§5.3, `es5`→`es2017`). All §6 accessibility findings are also fixed: the shared `Modal` now has proper dialog semantics (`role="dialog"`, `aria-modal`, `aria-labelledby`), auto-focus, a focus trap, and `Escape`-to-close (with new test coverage — `src/test/components/functional/Modal/Modal.test.tsx`); the checkout "Place Order" control is a real `<button>`; the hamburger menu and its full-screen backdrop are keyboard-operable (`Tab`/`Enter`/`Escape`); and the 4 jsx-a11y violations §5.2 surfaced (`PhoneNumberInput` label association, `PasswordInput` autocomplete, `UsernameInput` autoFocus, `GalleryPage` anchor content) are fixed too. Since those 44 §5.1 warnings turned out to be entirely jsx-a11y violations, fixing them for real here means **`CI=true npm run build` now succeeds outright** — `.github/workflows/ci.yml`'s build step no longer needs the `CI: false` workaround and now runs the standard `CI=true npm run build`. The §7 i18n/localization finding is also fixed: `i18next`/`react-i18next` now back every user-facing string in `src/main/**`, not just a representative slice (the user was explicitly asked and chose full extraction) — see "Internationalization (i18n)" below for the architecture. **Still open, per `AUDIT.md` alone:** ~72 remaining `npm audit` vulnerabilities in the `react-scripts`/CRA build toolchain at the time `AUDIT.md` was last updated, none critical (§2.4, further reduced by `AUDIT-2.md`, see below); the `Checkout.tsx` double-catch bug (§4.1, still open).

`AUDIT-2.md`'s fresh, independent pass surfaced 10 further findings across the same categories plus i18n (a completely unreachable profile-edit flow, a dead nav link, a wrong `<html lang>`, availability-check bugs, and more) — **all 8 of its sections are now fixed**, with three narrow, deliberate exceptions: **§1.7** (a real, still-open bug — `UserProfile.tsx`'s `GET /v1/account/me` failure handler passes a raw `Error` object to `setModalMessage` instead of its `.message`, and separately calls `setModalMessage` during render; discovered while adding test coverage under §4.1, not yet fixed — the one other correctness bug currently open, alongside `AUDIT.md`'s §4.1 double-catch above); **§2.2** (partially fixed — `npm audit`'s count dropped from 73 to 65 via targeted `package.json` `overrides` on three isolated leaf packages, with the rest blocked on either a non-functional `react-scripts@0.0.0` or a separate ESLint major-version migration); and two purely-informational, by-design items that were never defects (§7.3's untranslated internal `class-validator` messages on objects the app always constructs validly itself; §8.2's four backend endpoints documented but never called since no admin/order-history/ingredient-listing feature exists yet). Notably, `AUDIT-2.md`'s §2.3 fix is what closed `AUDIT.md`'s own previously-open §2.6 finding above — `react-router-dom` is now on `7.18.1` (a companion Node 18→20 bump was required, see `engines` in `package.json`), done with explicit user authorization given the breaking-change risk.

One thing the endpoint-path fix did **not** cover: request/response **body shapes** were only spot-checked against two documented endpoints, not exhaustively verified for all of them. `AUDIT-2.md` §8 later spot-checked a wider set (registration, password change, both address endpoints, both order endpoints) and fixed the one mismatch it found (`OrderModel` was sending an undocumented `isAuthenticatedUser` field), but this still isn't an exhaustive endpoint-by-endpoint verification. Don't assume a `fetch()` call's body shape is correct just because its URL matches `API_ENDPOINTS.md` — cross-check the body too before relying on it.

`AUDIT-3.md`'s third, independent pass found 17 further issues, concentrated in two places — five call sites sharing a genuine nested-`<form>` bug that silently discarded in-progress checkout/registration/profile-edit data on an Enter keypress, and a scattering of accessibility gaps mirroring fixes made *elsewhere* but missed on an identically-styled sibling component — and **all 5 of its sections are now fixed**: §1 Correctness bugs (9 findings — `FoodDetails.tsx`'s dead-end double modal on fetch failure now uses the shared `useModal()` context; `Menu.tsx`'s grid grouped items by 3 while its CSS was sized for 4; `Checkout.tsx`'s empty-cart redirect moved into a `useEffect`; `UsernameInput`/`EmailInput`'s availability-check cleanup was never actually registered, now fixed with a stale-response guard; plus message-content/typo/dead-code fixes); §2 Accessibility (6 findings — the `AddressInput.tsx`-owned nested `<form>` (plus two related handler-less/duplicate `<form>`s) converted to `<div>`s across all 5 affected flows; `ShoppingBagButton` made keyboard-operable to match its already-fixed `HamburgerMenuButton` sibling, the same pattern `AUDIT-4.md` §2.1 later found repeated on 7 *more* icon-only elements; `InputFieldCheckmark`/`InputFieldXmark` now take a required, context-specific `ariaLabel` instead of a hardcoded username-only default; `Modal` now restores focus on close; the mobile hamburger menu now has a real focus trap; `LoadingOverlay` now announces itself to screen readers); §3 Code quality (3 findings — `PersistSelectedStates` now uses `{ replace: true }` instead of pushing a duplicate history entry on every page load; `fetchCsrfToken` now validates the response actually contains a `csrfToken`; `MyUserModelBuilder`'s dead `password` field and its paired dead test fixtures removed); §4 Testing & coverage (3 findings — 18 new test files, 96 new tests, giving `converter/**`, `myDecorators/**`, and 7 previously-untested builders/models their first-ever coverage, each now at 100%); §5 Backend API contract (reviewed, no code change needed — its one finding, `checkPasswordIsCommon`, is covered below). Two informational, by-design items were left open (not defects): `checkPasswordIsCommon` (`src/main/utils/myUser/PasswordUtils.ts`) correctly calls the documented `POST /v1/registration/common-password` endpoint but is never actually invoked anywhere in the app — a fifth "documented but uncalled" backend endpoint alongside `AUDIT-2.md` §8.2's four; and `NavLinkPersist`/`NavigatePersist` (see "State management" below) carry real latent bugs but are unused anywhere in the app, so nothing currently reaches them.

`AUDIT-4.md`'s fourth, independent pass found 4 further issues — smaller in count than `AUDIT-3.md`'s 17 (three parallel research subagents hit a session limit mid-task and returned no usable output, so this pass's research was carried out directly instead) but including one genuinely serious, previously-undetected correctness bug — and **all 3 of its sections are now fixed**: §1 Correctness bugs (`CheckoutOrderSummarySection.tsx` mutated the `orderItems` prop array as a side effect of rendering; `<React.StrictMode>` — enabled app-wide — deliberately double-invokes render functions to catch exactly this class of bug, and here it silently duplicated every order line item submitted to the backend on every local dev checkout, invisible on screen since the *displayed* total was computed via separately-reset local variables; fixed by computing `orderItems` via a pure `useMemo` in `Checkout.tsx` instead); §2 Accessibility (7 icon-only links — `Header.tsx`'s 4 social-media links plus `ProfileButton`/`LoginButton`/`LogoutButton` — had no accessible name, the same "outlier among near-identical siblings" pattern caught elsewhere in this nav bar by `AUDIT.md` §6.3 and `AUDIT-3.md` §2.2, now fixed with a translated `aria-label` on each; `RegisterFormAddresses.tsx`'s "shipping same as billing" checkbox used a bare `<span>` (unlabeled, uncontrolled) where its Checkout sibling correctly used a `<label>`, now fixed — note the `checked` binding needed to be `!isShippingAddressFormDisplayed`, not a direct copy of Checkout's pattern, since Register's state variable represents the *opposite* of the checkbox's own meaning); §3 Content quality (`Footer.tsx`'s copyright line read "© 2024 Thes." instead of the app's real brand, "ImagineBar", now corrected).

## Commands

```bash
npm start          # run dev server (react-scripts start)
npm run build       # production build
npm test             # run tests in watch mode (react-scripts test / Jest + React Testing Library)
npm run test:coverage # single non-interactive run with coverage (react-scripts test --coverage --watchAll=false)
npm run lint          # eslint src --ext .js,.ts,.jsx,.tsx
npm run lint:fix       # eslint --fix
```

- Run a single test file: `npm test -- src/test/model/customer/AddressModel.test.tsx`
- Run tests matching a name: `npm test -- -t "should throw required error"`
- `npm test` runs in Jest watch mode by default (CRA); pass `CI=true npm test` for a single non-interactive run.
- `npm run test:coverage` enforces the `jest.coverageThreshold` floor configured in `package.json` (a regression gate set a few points below current actual coverage, not an aspirational target) — it fails if coverage drops below that floor.
- There is no separate typecheck script; `npm run build` (or `tsc --noEmit`) surfaces type errors since `tsconfig.json` has `noEmit: true`.
- `npm run lint` currently exits `0` with 0 errors (210 warnings, all `no-explicit-any` in `src/test/**`'s exhaustive model/builder/converter/decorator tests, tracked but non-blocking — the count grew from 182 as `AUDIT-3.md` §4 added first-ever test coverage for the `converter/`/`myDecorators/` layers using the same `as any` convention every other model/builder test already uses). Both `npm run build` and `CI=true npm run build` currently succeed — `src/main/**` has zero ESLint warnings, so CRA's CI=true warnings-as-errors mode has nothing to trip on (the remaining `src/test/**` warnings don't affect the build, since CRA's build-time lint only covers files reachable from the app's entry point).

Docker: `Dockerfile` builds with `node:20` (bumped from `node:18` alongside the `engines.node >=20` requirement `AUDIT-2.md` §2.3's `react-router-dom` upgrade introduced), then serves the static build via `nginx:stable-alpine` using `nginx.conf` (which also reverse-proxies API paths to the `app` backend service — see above).

## CI/CD & pre-commit hooks

- `.github/workflows/ci.yml` runs on every push/PR to `main`: `npm ci` → `npm run lint` → `npx tsc --noEmit` → `npm run test:coverage` → `CI=true npm run build`. The build step uses the standard `CI=true` mode (not overridden), so any new `src/main/**` ESLint warning fails it immediately rather than accumulating silently.
- `.husky/pre-commit` runs `npx lint-staged`, which runs `eslint` (check-only, no auto-fix) against staged `src/**/*.{js,jsx,ts,tsx}` files (config in `package.json`'s `"lint-staged"` key) — catches lint errors before they're committed, not just in CI.
- `.github/dependabot.yml` runs weekly update checks against the `npm`, `github-actions`, and `docker` ecosystems (the app's dependency tree, the CI workflow's own action versions, and the `Dockerfile`'s base images respectively) — the automated mechanism for surfacing newly-disclosed vulnerabilities going forward, rather than relying on someone remembering to re-run `npm audit` manually. The `npm` entry ignores **major**-version updates for `typescript` and `eslint` specifically (minor/patch bumps still flow through normally): both fail `npm ci` itself with an `ERESOLVE` peer-dependency conflict, not a type/lint error — `react-scripts@5.0.1` hard-pins `peerOptional typescript@"^3.2.1 || ^4"`, and `@typescript-eslint/eslint-plugin@5.62.0` (plus `react-scripts`'s own bundled `eslint-config-react-app`/`eslint-plugin-jest`) peer-depend on `eslint@"^6.0.0 || ^7.0.0 || ^8.0.0"` — so neither major bump is installable while this app stays on `react-scripts`, confirmed by two Dependabot PRs that failed CI this way before being closed.

## Architecture

For a deeper treatment of this section — full domain-model ERD, every endpoint's auth/CSRF requirements, sequence diagrams for the auth/registration/checkout flows, and the reasoning behind the Model→Builder→Converter→Utils pattern — see `docs/DESIGN.md`.

### Directory layout

- `src/app/` — Redux store setup (`store.ts`) and typed hooks (`useAppDispatch`/`useAppSelector` in `hooks.ts`).
- `src/main/pages/` — route-level page components (thin, one `index.tsx` per route) wired up in `src/App.tsx`'s `<Routes>`. **Naming note:** these `pages/*Page` directories (e.g. `pages/LoginPage`) are distinct from the similarly-named `components/page/*` directories below (e.g. `components/page/Login`) — a page is a thin route wrapper that renders the matching presentational component; don't confuse the two when navigating the tree.
- `src/main/components/` — reusable components, split into `page/` (page-specific sections like `Menu`, `Checkout`, `UserProfile` — see the naming note above), `input/` (form inputs grouped by domain: `customer/`, `myUser/`), `navigation-bar/`, `header/`, and `functional/` (behavioral wrappers: `AccountRouteGuard`, `Modal`, `LoadingOverlay`, etc).
- `src/main/features/` — only `footer/` remains here (a plain component, not a Redux slice). The CRA-template Redux "feature" folder pattern (slice + component colocated) this directory was originally for has been removed entirely — it's not a pattern used anywhere in the app.
- `src/main/model/`, `src/main/builder/`, `src/main/converter/`, `src/main/myDecorators/`, `src/main/utils/` — the domain model layer (see below); mirrored file-for-file by tests under `src/test/`.
- `src/main/context/` — React context providers (e.g. `ModalMessageContext` for a global message modal).
- `src/main/supports/` — cross-cutting helpers: `Persistence.tsx` (query-string state persistence + nav helpers), `fetch-utilities/` (CSRF token fetch).
- CSS is plain `.css` (Sass is a dependency but plain CSS is the convention seen), organized per-component in local `css/` subfolders plus shared styles in `src/main/css/`.

### Domain model pattern (the core architecture to understand)

Almost every domain concept follows the same four-layer pattern, and understanding one instance (e.g. `AddressModel`) explains the rest:

1. **Model** (`src/main/model/**`) — a class wrapping one value or composing sub-models. Validated in the constructor via `class-validator`'s `validateSync`, throwing the validation errors array if invalid. Composite models (e.g. `AddressModel`) hold nested leaf models (e.g. `ZipCodeModel`, `CityModel`); leaf models wrap a primitive (usually `string`) with decorators like `@NotBlank`, `@Matches`, `@Length`. Every model implements a manual `equals(other)` method (no `===`/deep-equal library). Custom decorators live in `src/main/myDecorators/` (`NotBlank`, `NotNull`, `NotUndefined`, `ValidPhoneNumber`, `NoZeroNorZeroSlash`), built with `class-validator`'s `registerDecorator`.
2. **Builder** (`src/main/builder/**`) — one builder class per composite model, with chainable `setX()` methods and a `build()` that calls the model constructor (and thus triggers validation). Used instead of large constructors/object literals to build composite models incrementally.
3. **Converter** (`src/main/converter/**`) — free functions that turn raw form data (a properly-typed inline object shape, e.g. from a `FormData`/registration form — not `any`) into a model instance, using the corresponding Builder.
4. **Utils** (`src/main/utils/**`, mirrored per model) — colocated constants and pure helpers for a given model: error message strings (e.g. `ERR_MSG_ZIP_CODE_REQUIRED`, resolved via `i18n.t('errors.ERR_MSG_ZIP_CODE_REQUIRED')` — see "Internationalization (i18n)" below), regexes, and validation constants referenced by both the Model and its tests.

Tests live in `src/test/**`, mirroring the `src/main/**` path of the thing under test (e.g. `src/main/model/customer/AddressModel.tsx` → `src/test/model/customer/AddressModel.test.tsx`). Test files exhaustively cover required/null/undefined cases for every validated field plus `equals()` behavior, using the shared assertion helper `expectErrorMessages` (`src/main/utils/test/ExpectErrorMessages.tsx`).

`src/test/components/**` (mirroring `src/main/components/**`) holds a different, lighter kind of test — component-level render/interaction smoke tests (mock `fetch` directly, assert on rendered text/DOM state and handler wiring) rather than exhaustive per-field validation, since the model/builder layer above already covers that. 14 components currently have coverage there (`AccountRouteGuard`, `Login`, `Register`, `Checkout`, `ShoppingCart`, `Modal`, `NavigationMenu`, `UsernameInput`, `EmailInput`, and the full `UserProfile` feature area — `UserProfile`, `PersonalData`, `BillingAddress`, `ShippingAddress`, `LoginData`); most other components still have none.

`converter/`, `myDecorators/`, and `utils/` files use `.ts` (they contain no JSX) — with one known exception, `src/main/utils/EmailUtils.tsx`, missed by the original rename sweep; `model/` and `builder/` files are still `.tsx` even though they also contain no JSX — both are known, deliberately out-of-scope gaps (AUDIT.md §3.7).

When adding a new domain field or model, follow this same Model → Builder → Converter → Utils (+ mirrored test) shape rather than introducing a different pattern.

### Internationalization (i18n)

`i18next` + `react-i18next` back every user-facing string in `src/main/**` (AUDIT.md §7, fixed). `src/main/i18n/i18n.ts` configures a single `hu` locale with one combined `translation` namespace, backed by `src/main/i18n/locales/hu/hu.json` — a flat-per-domain JSON file (e.g. `errors.*`, `home.*`, `checkout.*`, `userProfile.*`, plus a `phoneCountryNames` lookup used by `PhoneNumberInput`'s `react-phone-input-2` `localization` prop). There is no per-feature namespace split; organization is achieved entirely through nested JSON keys.

Two call patterns exist, chosen by **where the string is evaluated**, not by convention preference — using the wrong one breaks things:

- **Inside a function component** (the common case): use the `useTranslation()` hook — `const { t } = useTranslation()`, then `t('checkout.pageTitle')`. Interpolation uses `{{varName}}` placeholders matched to a second argument object, e.g. `t('errors.ERR_MSG_USERNAME_VALUE_LENGTH', { USERNAME_VALUE_MIN_LENGTH, USERNAME_VALUE_MAX_LENGTH })`.
- **At module-import time, outside any component** — this is required, not optional, for two cases: (1) every `ERR_MSG_*` constant in `src/main/utils/**`, because `class-validator` decorators (`@NotBlank({ message: ERR_MSG_X })`) evaluate their config object when the class file is *parsed*, before any component tree exists; and (2) free functions that aren't components but still throw user-facing errors (e.g. `Menu.tsx`'s `fetchMenuItems`, `ShoppingCart.tsx`'s `fetchFoodsByIds`). Both cases import the `i18n` singleton directly (`import i18n from '.../i18n/i18n'`) and call `i18n.t(...)` on it — `useTranslation()` cannot be used here since hooks only work inside components.

This only works because i18next is guaranteed to be initialized before either pattern runs: `src/index.tsx` imports `./main/i18n/i18n` as its **literal first line**, before any other import (so it initializes before anything that transitively imports an `ERR_MSG_*`-using utils file), and `src/setupTests.ts` imports it too, since Jest test files import components directly rather than going through `index.tsx` and would otherwise render with no i18next instance registered (symptom: a `react-i18next:: You will need to pass in an i18next instance` console warning and empty/missing translated text in test assertions). If you add a new `utils/**` file with an `ERR_MSG_*`-style constant, or a new free function that throws a user-facing error, follow the existing pattern in that directory (e.g. `src/main/utils/myUser/UsernameUtils.ts`) rather than hardcoding a literal.

Deliberately left untranslated: proper nouns/brand names (`ImagineBar`, social platform names), the footer's developer-attribution/copyright line, and image `alt`/attribution text already in English — translating those would be nonsensical regardless of locale.

### State management

Redux Toolkit (`@reduxjs/toolkit` + `react-redux`) is wired up via `src/app/store.ts` (`Provider` in `index.tsx`), but the store currently has **no reducers registered** — the CRA-template `counter` slice that used to be the only one was removed as dead code. All app state is local component state (`useState`) or the `ModalMessageContext`; the Redux plumbing is kept in place as scaffolding for future state, not because anything reads from it today. `PersistSelectedStates` (`src/main/supports/Persistence.tsx`) is a similar placeholder — it's structured to sync selected Redux state into the URL query string on change (see the comment inside it for where to add a key), but has nothing to persist yet. It still exposes `NavLinkPersist`/`NavigatePersist`/`useNavigatePersist` helpers that preserve the current query string across navigation — use these instead of `NavLink`/`useNavigate` directly when a component needs query params to survive navigation. Note `NavLinkPersist`/`NavigatePersist` (unlike `useNavigatePersist`'s own `mergeLink` helper) carry real latent bugs — naive string concatenation with the current search string rather than a proper `URLSearchParams` merge — but are currently unused anywhere in the app (`AUDIT-3.md` §3's informational note), so nothing exercises them today; fix the concatenation properly if you're the one who first wires either into a real component.

### Routing & auth

Routes are declared directly in `src/App.tsx` (`react-router-dom` v6). Authentication status is checked once on mount via `GET /auth-status` (`credentials: 'include'`) and passed down as `isAuthenticated`; `/account` is wrapped in `AccountRouteGuard`, which redirects to `/login` if unauthenticated. There's no auth context/hook — `isAuthenticated` and login/logout handlers live in `App.tsx` and are prop-drilled to `Nav`, `LoginPage`, and `CheckoutPage`.

## Linting conventions

`.eslintrc.json` enforces (beyond `eslint:recommended` / `plugin:react/recommended` / `plugin:jsx-a11y/recommended` / `@typescript-eslint/recommended`): 2-space indent, single quotes, **no semicolons**. `react/react-in-jsx-scope`, `react/prop-types`, and `@typescript-eslint/no-non-null-assertion` are disabled — non-null assertions (`!`) are used freely, especially in Builders' `build()` methods. All `jsx-a11y` rules run at the plugin's default `"error"` severity — none are downgraded. The one non-default rule config is `label-has-associated-control`'s `controlComponents: ["PhoneInput"]` option, needed because `PhoneNumberInput.tsx` wraps the third-party `react-phone-input-2` `PhoneInput` component (which renders its own internal `<input>` the linter can't otherwise trace to the wrapping `<label>`) — that `<label>`/`<input>` pair uses a real `htmlFor`/`id` association, not just a lint-satisfying workaround. If you add another third-party form-control component wrapped in a `<label>`, add it to that same `controlComponents` list rather than downgrading the rule.

For click handlers on non-interactive elements (`<div onClick>` etc.) that `jsx-a11y/click-events-have-key-events`/`no-static-element-interactions` would flag: check first whether the element is a genuine standalone control (give it `role`, `tabIndex`, and a matching `onKeyDown` — see `HamburgerMenuButton.tsx`) or something else — a full-screen modal/dropdown backdrop that should stay out of the tab order and close via `Escape` instead (see `NavigationMenu.tsx`), or a wrapper whose real interactive content is an already-keyboard-accessible child element like a `NavLink` (see `ShoppingBagDropdown.tsx`). Both of the latter two patterns suppress the specific rules with `eslint-disable-next-line` and a comment explaining which case applies, rather than bolting on a redundant fake `role="button"`.
